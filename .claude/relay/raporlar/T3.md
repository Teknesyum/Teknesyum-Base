# T3 · P0-B — sözleşme şeması · submitted

**Sürüm:** 2.57.0 · yayında · test 497 → 499 · commit `main`

## Ne değişti

- Yeni `teknesyum/hooks/contract-schema.js`: kimlik regex'i, durum enum'u, `durum()`,
  `bilinenDurum()`, `sozlesmeAdi()` tek kaynakta.
- `contract-guard.js` kendi `SIRA` tablosunu ve `durum()`'unu bıraktı, şemayı içe alıyor.
- `relay-watch.js`'teki üç `T*.md` beklentisi (satır 251, 521, 1168) şemaya bağlandı.
- `sealed` merdivene `done` ile aynı basamakta girdi.

## Neden

Dış denetim TB-003. `canonical()` `/^T[^/\]+\.md$/i` kullanıyordu. Depoda 22 açık
sözleşme var: D1-D4, E1, F1-F3, S1, S2, T1, U1-U11. 21'i `done/` kapısının, mühür
denetiminin ve durum merdiveninin tamamen dışındaydı. `sealed` tabloda olmadığı için
T1'in gerileme denetimi de sessizce atlanıyordu.

## Kanıt

Düzeltmeden önce çalışma ağacında doğrulandı. İki yeni regresyon testi:
`T disindaki sozlesme kimlikleri de done/ kapisiyla korunur` (D1, E1, F3, S2, U11) ve
`done/ disinda kalan yol kapiyi tetiklemez`.

## Kalan

T4 (CI hermetikleştirme), T5 (`/rc` enjeksiyonu), T6 (`/ozel` yalan başarı) açık.
T4'ün ucuz kısmı: beş UI suite'ini `npm test`e bağla, iki lint hatasını düzelt.
