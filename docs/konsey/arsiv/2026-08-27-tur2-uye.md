# Koşu 2026-08-27 · tur 2 · üye (opus) — birebir

## (a) Süreç ağacı + transkript bağlama

Asgari yama üç dokunuş, `scripts/bench/kos.js`: `:7` spawnSync'i içeri al; `:101` öncesine
`agaciOldur(c)` yardımcısı (`taskkill /PID <pid> /T /F`, başarısızsa `c.kill()` yedeği);
`:113-116` `c.kill()` yerine `agacOldu = agaciOldur(c)`, sonucu `:125` çözüm nesnesine ve
`:464` günlüğüne yaz. Bugünkü `c.kill()` Windows'ta yalnız tutamaç sahibini öldürüyor,
`claude`'un açtığı rg/git/hook node süreçleri öksüz kalıyor — `defter.jsonl:41` B1 borcu.

Kırdıkları:

1. **Planlı kesinti geçersizlik damgası yiyor — kritik.** `:463` `sonuc.tavanAsildi =
   r.kesildi`, `topla.js:156` bunu geçersizlik nedeni sayıyor. Tasarım "sabit sürede kill"
   ise her koşu tavan aşar, hepsi elenir, birincil metrik n=0. A0 kapandığı için rapor
   artık "hepsi geçti" demez, "geçerli koşu yok" der. Çare iki satır: `sonuc.planliKesinti
   = true` ve `topla.js:156` -> `if (ham.tavanAsildi && !ham.planliKesinti)`.
2. **`/F` transkriptin son satırını yarım bırakır.** `transkriptOzeti:344-347` bozuk JSON
   satırını sessizce atlıyor; kayıp tam da kesme anına denk gelen tur. Çare: `o.bozukSatir++`
   sayacı, sıfırdan büyükse rapora şerh.
3. **İkinci oturum "alt ajan" sayılır.** `transkriptBul:330-335` en yeni jsonl'i seçiyor,
   `topla.js:284-297` kalanları `alt`'a topluyor — ajan sayısı sütunu şişer. Düşük öncelik,
   `alt`'a yalnız `subagents/` altını alarak tek satırda düzelir.

Çapraz kirlenme riski yok: `konfigKur:292-296` her koşuda konfig kökünü siliyor, öksüz süreç
kendi konfigine yazıyor.

Emin olmadığı yer: Bash kapalı olduğu için yamayı koşturamadı; `taskkill` dönüş kodlarını ve
`/T /F` sonrası `close` olayının tetiklendiğini varsaydı. Sentetik uzun süreç eşliğiyle
`--kendi-testi`'ye eklenmeden bench'e sokulmamalı.

## (b) A1 alınmadan birincil metrik güvenilir mi

**Evet — hatta A1 alınırsa metrik bozulur. Bench öncesi alınmamalı.**

Metrik "mühürlenip bağımsız doğrulamada düşen sözleşme oranı" ise, ölçtüğü olay A1'in
kapattığı deliğin ta kendisi. A1 bunu gözlemlenebilir sonuçtan koşu ortasında sert bloğa
çevirir: headless ajan `complete` reddini görüp yeniden dener, tur yakar, tavana yaklaşır —
(a)'daki geçersizlik sorununu besler. A1'in headless'ta hiç çalışma zamanı yok.

Metrik koşu sonrası **salt-okunur doğrulayıcıdan** türetilir. Her koşunun relay durumu
diskte: `sonuc.calisma` + `/.claude/relay`, `kosu()` fixture'ı oraya kopyalıyor (`:423-424`)
ve silmiyor. Sözleşme başına dört kontrol:

1. bitmiş sözleşme klasöründe `<ID>.md` var mı;
2. `audits/<ID>-<round>.used.json` var mı (yoksa `complete` hiç koşmamış — kanca altından
   geçilmiş);
3. `defter.jsonl`'de `id` satırı var mı ve `kaynak !== 'devralindi'` mi. Kanıt bu depoda:
   `defter.jsonl` 1, 19, 20, 21'de `T2` dört kez `devralindi`, `T2-*.used.json` hiç yok,
   sözleşme yine de bitmiş sayılıyor;
4. `used.json`'daki `auditorRunId` -> `live/<id>.json` çözülüyor mu, `agent_type` auditor mı,
   `files` boş mu; `diffHash`, `completedSha` anındaki `dosyaOzeti(kok, owns)` ile tutuyor mu.

1 doğru + (2,3,4)'ten biri yanlış = düşen mühür. Payda bitmiş sözleşme toplamı. Yaptırımı
değil teşhisi çalıştırır: bu gece yazmak sıfır davranış riski, A1 sonrası da aynı sayıyı
üretir.

Netleştirme: başkanın `live/` boşaltma önkoşulu bu metriği etkilemiyor — bench
`CLAUDE_CONFIG_DIR=konfig` ve `benchKok/<ad>/calisma` altında koşuyor, depo kökündeki
`.claude/relay/live` ölçüme girmiyor. İki karar bağımsız.
