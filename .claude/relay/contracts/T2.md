---
id: T2
status: submitted
round: 2
owns: [teknesyum/hooks/dil.js, teknesyum/hooks/relay-watch.js, teknesyum/scripts/premium.js, teknesyum/skills/relay/SETTINGS.md, teknesyum/agents/advisor.md, teknesyum/agents/auditor.md, teknesyum/agents/builder.md, teknesyum/agents/planner.md, teknesyum/agents/scout.md, teknesyum/agents/scribe.md, teknesyum/agents/ui-builder.md, teknesyum/commands/premium.md, test/run.js]
---

# T2 · Düğmeler enjeksiyona, ajan dosyaları tabana

Dalga 2'nin ikinci taşı. `T1` profili oturuma bağladı ama **profilin etkileri hâlâ makine
geneli**: ajan frontmatter'ı ve `SETTINGS.md` her oturum için aynı dosyaya yazılıyor.

`T1` bunu gizlemiyor — `durum()` her koşuda "ajan dosyaları: X — makine geneli, oturuma
izole değil" diye söylüyor. Bu sözleşme o satırı gereksiz kılacak.

## Ölçülmüş kısıtlar — yeniden araştırma

1. **Çağrı anındaki `model` frontmatter'ı eziyor.** Tek bir `teknesyum:planner` tanımıyla
   aynı anda `claude-fable-5` ve `claude-opus-5` açıldı. Kayıtlar `.claude/relay/live/`
   altında: `a39cab41e2c7eb20b.json`, `ab06694668ec6ce7d.json`.
2. **`effort` çağrı anında geçilemez.** `Agent` şemasında alan yok; değer yalnız ajan
   tanım dosyasından geliyor. Kayıtlarda birebir görünüyor: advisor `low`, builder
   `xhigh`, scribe `low`.
3. **`T1` `profil(sid)` API'sini açtı.** `dil.js` içinde. Oturum kaydı
   `<konfigKok>/teknesyum/oturumlar/<sid>.json`.

(2)'nin sonucu: **efor izole edilemez.** Bu sözleşme onu çözmeye çalışmaz — tabana
sabitler ve neden orada kaldığını yazar.

## Yapılacaklar

### 1 · `ajanlariYaz()` kalkar, ajan dosyaları taban olur

Yedi ajan dosyasının `model` ve `effort` alanları **`normal` profilin değerlerinde donar**.
`maxTurns` de aynı şekilde.

Efor tabanının `normal` olması konsey kararı: tek taban kalınca eco `xhigh` öderse eco
anlamını yitirir, premium `medium`'da kalırsa premium anlamını yitirir. Premium farkını
`model` taşır — model birinci dereceden kaldıraç, efor ikinci.

`model:` alanını dosyalardan **tümden sil**. Konsey riski şuydu: dosyadaki `model` çağrı
parametresini ezerse mekanizma sessizce çöker. Ölçüm ezmediğini gösterdi, ama alan hiç
yoksa ezme ihtimali de yok. Alan yoksa harness varsayılana düşer, T0 zaten her çağrıda
model geçecek.

`premium.js` artık ajan dosyalarına **hiç yazmaz**. `profilAdi()`'nin dosya karşılaştırması
ve `durum()`'un "ajan dosyaları: X" satırı kalkar.

### 2 · `dugmeleriYaz()` kalkar, düğmeler enjeksiyona geçer

`SETTINGS.md` **makine varsayılanı** olarak donar — `normal` profilin değerleri.
`premium.js` ona da yazmaz.

Profile göre değişen düğmeler `UserPromptSubmit` enjeksiyonuna girer. **Yalnız tabandan
sapanlar** yazılır, tam liste değil.

Bu şart ölçümden geliyor: `docs/OLCUM-TABAN.md` farkın %89'unun konuşma hacminden
geldiğini buldu. Tam listeyi her isteme yazmak tam da o kalemi büyütür. eco'da sapma
3-4 satır etmeli.

### 3 · Kod tarafı üç katman okur

`relay-watch.js` içindeki `ayarSayi()` (bugün proje `SETTINGS.md` → global) üç katmana
çıkar: **oturum profili → proje `SETTINGS.md` → global**.

`agent_stall` ve `agent_loop` gibi düğmeleri model değil kanca okuyor; onları metne
taşımak kimsenin okumadığı satır üretir. Kod tarafı dosyayı okumaya devam eder, üstüne
oturum profilini uygular.

### 4 · Profil değişince enjeksiyon yenilenir — `Ö4`'ün bulduğu hata

Bugün enjeksiyon ilk iki istekle sınırlı (`sayacGecti`). Profil üçüncü istekte değişirse
bağlamdaki metin eski profilde kalıyor.

Bench'te tam olarak bu oldu: `/premium eco` görev isteminden sonra çalıştı ve bağlam
**72 tur boyunca premium metnini taşıdı**. O koşu eco'yu hiç ölçmedi.

Sayaç profil değişiminde sıfırlanır. Ölçüsü: profil değiştirdikten sonraki ilk istekte
yeni bloğun geldiğini transkriptten görmek.

### 5 · Tur makbuzu adı ayrışır

`Ö4` bir raporlama karışıklığı buldu: bir koşunun `Stop` satırı ~313.500 token diyor,
harness bütçe sayacı 171.114. Base'in tahmini alt ajanları sayıyor, sayaç saymıyor.
**İkisi de "Tahmini Token" adıyla görünüyor.**

Base'in bastığı sayının neyi saydığı adından anlaşılsın. Metin `dil.js`'ten.

### 6 · Belge

`commands/premium.md`: "Betik üç yeri birden yazar" artık tümden yanlış — hiçbir yere
yazmıyor, oturum kaydı dışında. `SETTINGS.md` başına tek satır: bu dosya makine
varsayılanıdır, profil onu ezmez.

## Kabul kriterleri

1. `/teknesyum:premium premium` sonrası `git status` **temiz** — hiçbir dosya değişmez.
2. İki farklı `CLAUDE_CODE_SESSION_ID` altında `ayarSayi('parallel_width')` farklı değer
   döner.
3. `durum()` çıktısında "ajan dosyaları" ve "relay düğmeleri" uyuşmazlık satırları
   **kalkar** — uyuşmazlık artık mümkün değil.
4. eco'nun enjeksiyonu `normal`'inkinden **küçük**. Bayt olarak ölç ve rapora yaz.
5. Profil değiştikten sonraki ilk istekte yeni enjeksiyon bloğu gelir.
6. Ajan dosyalarında `model:` alanı yok; `effort` ve `maxTurns` `normal` değerlerinde.
7. Tur makbuzunun adı neyi saydığını söyler.
8. 296/296 test geçer. Yeni davranışların her biri için test yaz.
9. `npx --yes @biomejs/biome check --write` dokunduğun js dosyalarına.

## Mutasyon disiplini

En az üç kritik test için: düzeltmeyi geçici geri al, testin **kaldığını** doğrula, geri
koy. Kriter 2, 4 ve 5'i mutlaka sına — üçü de sessizce bozulabilir.

## Yasaklar

- Kod yorumu yazma.
- `hooks/ekran-kapisi.js` ve `hooks/dil.js`'in ekran kapısı metinlerine dokunma —
  E1 deneysel özelliği, ayrı durur.
- Profil adlarını, komut adını, `TEKNESYUM_PREMIUM` davranışını değiştirme.
- Eforu izole etmeye çalışma — çözülemez olduğu ölçüldü.
- Enjeksiyona tam düğme listesi yazma. Yalnız sapanlar.

## Rapor

**Tur 2 — denetim kaldı, dört madde kapatıldı.** `315/315` test geçiyor.

### Ne değişti

`premium.js` artık hiçbir depo dosyası yazmıyor. `ajanlariYaz()`, `dugmeleriYaz()`,
`alanYaz()`, `ayarYolu()`, `dugmeProfili()` ve `profilAdi()` silindi. Yerine `TABAN`
sabiti (`normal`), `sapmalar(profil)` ve `sapmaSatiri(profil)` geldi. CLI gövdesi
`main()` içine alınıp `require.main === module` arkasına kondu; `PROFIL`, `DUGME`,
`TABAN`, `KANCA_DUGME`, `sapmalar` ve `sapmaSatiri` dışa açıldı. `tarama.js` eşikleri
hâlâ `premium.js` kaynağından metin olarak okuyor, o yol değişmedi.

Yedi ajan dosyasından `model:` alanı tümden silindi. `effort` ve `maxTurns` `normal`
profilin değerlerinde donduruldu. `SETTINGS.md`'nin değerlerine dokunulmadı; başına
makine tabanı olduğunu ve üç katmanlı okuma sırasını söyleyen bir bölüm eklendi.

`relay-watch.js`: `ayarSayi()` önce oturum profilinin sapmasına, sonra proje
`SETTINGS.md`'sine, sonra globale bakıyor. Sapmayan düğmede profil katmanı susuyor —
yoksa proje dosyası hiçbir düğmede sözünü geçiremezdi. `hatirlat()` enjeksiyona
`sapmaSatiri(profil())` ekliyor. `sayacGecti()` sayacın yanına profili yazıyor, profil
değişince sıfırdan başlıyor. `kimlikDenetle()` beklenen modeli artık profilden türetiyor.

`dil.js`: `turOzeti` adı ayrıştı, `dugmeSapma` eklendi, `ecoNotu` kısaldı.

### Denetim maddeleri

**1 · Model sessiz düşüşü — kapatıldı.** Doğru teşhis: `t.model` boşalınca bekçi
kalkmıştı ve `test/run.js` bu sessizliği kilitliyordu. `beklenenModel(rol)` eklendi,
`PROFIL[profil()][rol].model` döndürüyor; `bekle` sırası artık **çağrı modeli → profil
modeli**. Çağrı modeli geçilmediğinde profilin modeli beklenir, uyuşmazlık yine yakalanır.
`tanimOku()` de sadeleşti, artık yalnız `effort` okuyor — dosyada olmayan alanı okumaya
çalışmıyor.

Sessizliği kilitleyen test silindi. Yerine üç test geldi: beklenen modelin profilden
türetilmesi (`normal`de `scribe` haiku bekler), premium oturumunda sonnet ile açılan
`builder`ın bildirilmesi, ve çağrıda geçilen modelin profil beklentisini ezmesi.

**2 · eco istek başına maliyeti — kapatıldı.** Haklı: bildirdiğim sayı oturum toplamıydı,
istek başında eco (912) normalden (645) pahalıydı. `ecoNotu` 532 → **231** karaktere
indi. Çıkarılanlar zaten başka yerde yazılı olanlardı: "tek ajan varsayılandır"
(`parallel_width 1`, sapma satırında), "cevabı tek cümle yaz; tablo, madde, ayrıntı"
(`report_length short` + `briefing quiet`), "`rg`, `sed`, `biome` işi görüyorsa model
çağırma" (`SETTINGS.md` üç profilde de geçerli diyor, `RULES.md` de aynı kuralı taşıyor),
"hangi satır aralığının gerektiğini bilmeden açma" (aynı cümlenin uzun hâli). Kalan:
öncelik cümlesi, grep-önce kuralı, `Explore` yasağı, ajan gerekçesi, kısa cevap.

**3 · İki `sapmaMetni` — kapatıldı.** Biçimlendirme tek gövdede: `premium.js` içindeki
`sapmaSatiri(profil)`. `KANCA_DUGME` filtresi de orada, tek yerde. `relay-watch.js` onu
`require` ediyor, kendi kopyası kalmadı. Ayrım kasıtlı: `sapmalar()` **bütün** sapmaları
döndürür ve `ayarSayi` onu kullanır — sözleşme §3 "kod tarafı üstüne oturum profilini
uygular" diyor, kanca düğmesi bir gün saparsa `agent_stall` de profilden gelmeli.
Filtre yalnız **metne** uygulanır.

**4 · Kod yorumları — silindi.** 34 satır: `premium.js` `TABAN` bloğu, `relay-watch.js`
üç blok (`ayarSayi` katmanları, `KANCA_DUGME`, `sayacGecti`/Ö4) ve stdin `require.main`
notu, `dil.js` iki blok (`turOzeti`, `dugmeSapma`), `test/run.js` on bir blok. Gerekçeler
bu rapora taşındı. Mevcut bir yorumu kısalttığım tek yer `TANIM_DIZINI` bloğu — orada
artık yanlış olan iki satır çıkarıldı, yeni satır eklenmedi.

**Küçükler.** `commands/premium.md:137` "eco 5 depo" → "eco 1 depo". `durum()` ajan
satırı `çağrı opus · efor medium · tur 60` biçimine geçti; model değerinin çağrıdan,
efor ve turun dosyadan geldiği artık satırın kendisinden okunuyor. `test/run.js`'teki
iki `toplam` yardımcısı tek `enjeksiyonOlcu(p, ayar)` gövdesine indi, `{tek, toplam}`
döndürüyor.

### Ölçüm — kabul kriteri 4

| Profil | İstek başı | Oturum toplamı (tavanla) | Sapma satırı |
|---|---|---|---|
| eco | **611** | **611** | 132 |
| normal | **645** | **1290** | yok |
| premium | 2636 | 5272 | 231 |

eco her iki ölçüde de normalden küçük: istek başına %5, oturum başına %53 daha az.
Marj istek başında dar (34 bayt) — `ecoNotu`'na yeni cümle eklenirse ilk kırılacak test
`eco istek basina da normalden az bayt tutar` olur, bu kasıtlı.

### Doğrulama

**Kabul kriteri 1 iki yönden sınandı.** Değişiklikler `git stash`'e alınıp temiz ağaçta
`node premium.js premium` koşturuldu: yedi ajan dosyası ve `SETTINGS.md`, toplam sekiz
dosya değişti — bugünkü hata birebir üretildi. Düzeltme geri konup aynı komut
koşturulunca `git status --short` çıktısı öncesi ve sonrası birebir aynı kaldı, yeni
izlenmeyen dosya da oluşmadı. İkinci turda tekrarlandı, sonuç aynı.

**Mutasyon disiplini — altı mutasyon, altısı da yakalandı:**

| Konu | Mutasyon | Kalan test |
|---|---|---|
| Kriter 2 | `ayarSayi`'dan oturum profili katmanı çıkarıldı | `iki oturum kimliginde parallel_width farkli deger doner` (2≠1), `sapmayan dugmede proje SETTINGS.md hala gecerli` (7≠1) |
| Kriter 4 | `sapmaSatiri` sapma yerine tam `DUGME` listesi yazdı | `enjeksiyona yalniz tabandan sapan dugmeler girer`, `taban profil enjeksiyona dugme satiri yazmaz` |
| Kriter 4 | tam liste yalnız eco'ya verildi | `eco oturum basina normalden az karakter enjekte eder` (0,83), `eco istek basina da normalden az bayt tutar` (sapma 287), `enjeksiyona yalniz tabandan sapan dugmeler girer` |
| Kriter 5 | `sayacGecti` yalnız sayı tutan hâline döndü | `profil degisince enjeksiyon sayaci sifirlanir` |
| Denetim 1 | `bekle` yeniden yalnız çağrıdan türetildi | `cagri model gecmediyse beklenen model profilden turetilir`, `premium oturumunda sessiz model dususu yakalanir` |
| Denetim 2 | uzun `ecoNotu` geri kondu | `eco istek basina da normalden az bayt tutar` (912 / 645) |

Her mutasyondan sonra dosya geri kondu ve suite yeşile döndürüldü.

### Sayı ve dal durumu

Worktree `8f85446` üzerinde açılmıştı, orada taban `282`'ydi. `main` iki kez ilerledi ve
iki kez merge edildi; ikisinde de çakışma çıkmadı. İkinci merge `main`'in T2 tur 1'i
zaten aldığı noktadan (`06a22ff`) yapıldı, yani bu daldaki fark artık **yalnız denetim
düzeltmeleridir**. Son sayı **315/315**. `E1`'in ve imza bloğunun `dil.js`'teki
metinlerine dokunulmadı.

`npx @biomejs/biome check --write` dokunulan dört js dosyasına iki turda da koşturuldu.

### Kapsam notları

- `dil.js` ilk turda `owns` dışındaydı; sözleşme gövdesi §5 oraya yazdırdığı için
  dokunuldu ve `live/_sorun.log`'a düşüldü. İkinci turda `owns`'a eklendi.
- `relay-watch.js`'e `module.exports = { ayarSayi }` ve stdin bağlamasının `require.main`
  koruması eklendi — kabul kriteri 2 fonksiyonun yan etkisiz çağrılabilmesini gerektirdi.
  Kanca olarak koşarken davranış aynı.
- Kapsam dışı, düzeltilmedi: `README.md` hâlâ betiğin üç yeri birden yazdığını anlatıyor
  ve tur makbuzunu eski adıyla gösteriyor (satır ~571). `owns`'ta değil.
- `/premium` bu oturumun profil kaydını `premium` bırakıyor; makine varsayılanı da
  `premium`, yürürlükteki profil değişmedi.
