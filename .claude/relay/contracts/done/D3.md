---
id: D3
status: submitted
round: 2
owns: [teknesyum/hooks/hooks.json, teknesyum/hooks/relay-watch.js, teknesyum/hooks/dil.js, teknesyum/scripts/statusline.js, teknesyum/skills/relay/references/multi-session.md, teknesyum/skills/relay/SKILL.md, teknesyum/commands/report.md, test/run.js]
---

# D3 · Kesinti kaybolmasın, yönlendirme dikkat dağıtmasın

**`D1` ve `D2` bitmeden başlamaz** — `relay-watch.js`, `dil.js`, `test/run.js` onlarda.

## Kullanıcının isteği

Üç parça: T0 neyi yaptığını ve neyin açıkta olduğunu rutin tutsun · ajanla iki yönlü
iletişim olsun · yönlendirme ajanın dikkatini dağıtmasın.

## Fable'ın görüşü — alındı

Tek cümlelik iskelet: **durum diskte durur, bağlama yalnız işaretçi girer.**

Gerekçe ölçülmüş: `docs/OLCUM-TABAN.md` bir oturumun maliyetinin **%89'unun konuşma
hacminden** geldiğini buldu. Her tura durum bloğu basmak, az önce bir dalga harcayıp
küçülttüğümüz kalemi büyütür.

**Reddettikleri de alındı:** her turun başına açık iş bloğu enjekte etmek · ajanlara
gelen kutusu/yoklama altyapısı kurmak (`SendMessage` + `live` zaten boru, eksik olan
protokoldü) · kesintileri erteleyip toplu okumak.

Son maddenin gerekçesi aynen: *"geciken okuma bu oturumdaki iki merge kazasının ilacı
değil, ikizidir."* Aciliyet kararı makineye verilmez.

## T0'ın ölçtükleri — fable "bilinmiyor" demişti, ölçüldü

1. **`PostToolUse` `SendMessage` için ateşleniyor.** Kontrol: Bash başına tam `+1`
   (3491→3492); arada `SendMessage` olan aralık `+2`. Mekanik iz kurulabilir.
2. **Tur ortası kesinti `UserPromptSubmit` ateşlemiyor.** Bu oturumun kaydı kanıt:
   tur başı mesajlar enjeksiyon bloğuyla geliyor, tur ortası kesintiler yalnız
   `system-reminder` sarmalıyla. **Fable'ın umduğu kanca noktası yok.**

(2)'nin sonucu: kesinti anına kanca bağlanamaz. Geri çekilme hattı olan `Stop` kancası
tek yol ve bu sözleşme onu kurar.

## Yapılacaklar

### 1 · `live/_acik.json` — üç alan, tavan 10 satır

`simdi` (tek satır, yürüyen iş) · `acikta[]` (cevaplanmamış kesintiler) · `sirada`
(tek satır, sonraki adım).

Kalıcı durum `ROTA`'da kalır. Bu dosya **yalnız oturum içi kuyruktur** — iki kayıt
yeri olmasın.

### 2 · Kesintinin üçe ayrılması — T0 disiplini, `SKILL.md`'ye

Kesinti geldiğinde:

| Durum | Ne yapılır |
|---|---|
| Tek satırda cevaplanır | Cevapla, geç. Kayda girmez. |
| Yürüyen işi değiştirir | Dur, işi değiştir. |
| İkisi de değil | `acikta`'ya yaz, kullanıcıya tek satır: kuyruğa alındı |

Kesinti **ertelenmez**, okunur ve sınıflanır. Fable'ın ayrım noktası bu.

### 3 · `Stop` kancası — okunma anı

Tur biterken `acikta` boş değilse **tek satır** basar: kaç madde açıkta.

Ölçü satırı emsali — o da `Stop`'tan geliyor ve bağlamı şişirmiyor.

### 4 · Statusline — bağlam maliyeti sıfır

`açıkta N · ajan X/Y` eklenir. Tur makbuzu zaten statusline'a taşınmış, emsal var.
Bağlama girmez, kullanıcı bakınca görür.

### 5 · Yönlendirme biçimi — `multi-session.md`

Ajana giden mesaj:

- **İlk kelime fiil:** `DUR` · `DEVAM` · `DEĞİŞTİ`
- **Tavan 5 satır** — işçinin dönüş tavanıyla simetrik

**Beş satıra sığmayan şey yönlendirme değildir.** Sözleşme dosyasına yazılır, ajana
tek satır gider: *"sözleşme güncellendi, §X'i yeniden oku."* Okunma anı sözleşmedir,
mesaj değil.

Bu kural T0'ın bu oturumdaki davranışını doğrudan yasaklıyor — ajanlara 40 satırlık
düzeltme mesajları yazıldı. Kural o pratiği bitirmek için var.

**Tetik:** kullanıcının sözü yürüyen ajanın `owns`'una ya da sözleşmesine dokunuyorsa
**hemen** iletilir (boşa iş önlenir); dokunmuyorsa `acikta`'ya girer ve sonraki
sözleşmeye işlenir.

### 6 · Mekanik iz — `PostToolUse:SendMessage`

Her yönlendirme ajanın `live/<id>.json` kaydına `steered[]` olarak düşer: zaman, ilk
kelime, satır sayısı, metin.

**5 satır aşımında `decision: block`** ve gerekçe. Kural mekanik denetlenmezse ölü doğar
— bu deponun beş günlüğünün ortak sonucu.

Sürdürülen ajan `steered[]`'den kendi geçmişini okuyabilir.

### 7 · `/report` — `acikta`'yı da bassın

Açık madde varsa listeler. Dalga sonu ve kapanış raporu `acikta` boşalmadan kapanmaz.

## Kabul kriterleri

1. `_acik.json` üç alanı tutar, 10 satırı geçmez.
2. `Stop` kancası `acikta` boş değilse tek satır basar, boşsa susar.
3. Statusline `açıkta N · ajan X/Y` gösterir.
4. 5 satırı geçen `SendMessage` **engellenir**, gerekçe döner.
5. Geçen yönlendirme `steered[]`'e düşer; kayıtlı sayı = atılan sayı.
6. `/report` açık maddeleri listeler.
7. Bağlama hiçbir turda durum bloğu basılmaz — yalnız `Stop`'un tek satırı.
8. `node test/run.js` geçer.
9. `npx --yes @biomejs/biome check --write` dokunduğun js dosyalarına.

## Mutasyon disiplini

Kriter 2, 4 ve 7 şart. 2 bozulursa kesinti sessizce kaybolur, 4 bozulursa kural ölü
doğar, 7 bozulursa ölçülmüş baskın kalem büyür.

## Yasaklar

- Kod yorumu yazma.
- Her tura durum bloğu enjekte etme.
- Kesintiyi erteleme ya da toplu okuma.
- Ajanlara yoklama/gelen kutusu altyapısı kurma.
- `acikta` ile `ROTA`'yı ikiz kayıt yapma.

## Kayıt noktası

Dokuz kabul kriteri de karşılandı, iş bitti. Kalan bir şey yok.

## Rapor

### Ne yapıldı

**`live/_acik.json` — üç alan, tavan 10 satır** (`relay-watch.js`). `acikNormal()` gelen
nesneyi üç alana indirir: `simdi` ve `sirada` tek satıra sıkıştırılır (200 karakter tavan),
`acikta[]` sekiz maddeye kırpılır. 1 + 8 + 1 = 10 satır. Fazladan alan atılır.

Kırpma mekaniktir: `PostToolUse` `Write|Edit|NotebookEdit` olayında dosya adı `_acik.json`
ise `acikKirp()` dosyayı normalleştirip geri yazar. Model ne yazarsa yazsın dosya tavanda
kalır.

Dosya `acikGovde()` ile yazılır — `acikta` tek satıra dizilir, dosya her zaman **4 fiziksel
satır**. "10 satır" ölçütü hem içerik hem dosya olarak okunsa da geçer; `JSON.stringify(...,2)`
ile sekiz madde 14 fiziksel satır ederdi.

**`Stop` kancası** (`relay-watch.js` → `acikBildir`). `Stop` dalında `turBitir`'den önce
çağrılır: `acikta` boş değilse `duyur()` ile tek satır — `Teknesyum ▸ Açıkta N madde —
listesi /report ile açılır.` Boşsa ya da dosya yoksa hiç çıktı yok. Madde **metinleri**
basılmaz, yalnız sayı.

**Statusline** (`statusline.js`). İkinci satıra `açıkta N · ajan X/Y` eklendi. `N` =
`_acik.json` → `acikta.length`, `X` = `_running.json` içindeki taze kayıt, `Y` =
`max(ajan kaydı, X)`. `N` sıfırsa `açıkta` parçası hiç yazılmaz; ajan yoksa `ajan` parçası
yazılmaz. `live`, `cs`, `ags` ikinci satırdan önce bir kez hesaplanıyor — aşağıdaki
tekrar eden okumalar kaldırıldı, davranış aynı.

**5 satır tavanı — mekanik** (`relay-watch.js` → `yonlendirmeIzi`). `PostToolUse` +
`tool_name === 'SendMessage'`. Mesaj `message|text|prompt|content` alanlarından okunur,
sondaki boşluk kırpılıp satır sayılır. 5'i aşarsa `decision: block` + gerekçe
(`dil.js` → `yonlendirmeTavan`, tr/en): tavanı söyler ve doğru yolu verir — değişikliği
sözleşmeye yaz, ajana tek satır gönder.

**Mekanik iz** (`steeredYaz`). Geçen her mesaj hedef ajanın `live/<id>.json` kaydına
`steered[]` olarak düşer: `t`, `fiil` (ilk kelime), `satir`, `metin` (600 karakter tavan),
son 20 kayıt. Engellenen mesaj **kayda girmez** — atılmamıştır; "kayıtlı sayı = atılan
sayı" böyle tutuyor.

Hedefin kaydı yoksa (`agent_id` gelmedi ya da dosya açılmamış) yönlendirme `_steered.json`
dosyasına düşer. Alt çizgili ad bilinçli: statusline ve `oturumKapat` `_` ile başlayan
dosyaları ajan saymaz, yoksa yönlendirme tek başına hayalet bir ⨯ ajan satırı doğururdu.

**Belgeler.** `SKILL.md` §1.1.1 — kesintinin üçe ayrılması, kuyruk dosyasının üç alanı,
rota ile ikiz kayıt yasağı, "durum bağlama basılmaz" gerekçesiyle. `multi-session.md` §5.3
— ilk kelime fiil, tavan 5 satır, beş satıra sığmayan şeyin sözleşmeye yazılması, hemen
gönderme tetiği (`owns`/sözleşmeye dokunuyorsa), `steered[]`. `report.md` — `_acik.json`
okunacaklar listesine girdi, `AÇIKTA` bölümü örnek çıktıya eklendi, dalga sonu ve kapanış
raporunun `acikta` boşalmadan kapanmayacağı yazıldı.

### Kabul kriterleri

| # | Durum | Nasıl doğrulandı |
|---|---|---|
| 1 | geçti | `_acik.json uc alan tasir, on satiri gecmez` — 15 madde 8'e, çok satırlı `simdi` tek satıra indi, dosya 4 satır |
| 2 | geçti | `Stop kancasi acik madde varken tek satir basar, boska susar` — dolu/boş/dosyasız üç hâl |
| 3 | geçti | `statusline acikta N · ajan X/Y gosterir` + madde yokken yazmadığı ayrı testte |
| 4 | geçti | `5 satiri gecen SendMessage engellenir, gerekce doner` — `decision: block`, gerekçede tavan ve doğru yol |
| 5 | geçti | `gecen yonlendirme steered[] e duser` — 3 atıldı, 1'i engellendi, kayıt 2 |
| 6 | geçti | `/report acik maddeleri listeler` — `report.md` metni denetleniyor |
| 7 | geçti | `acik is listesi hicbir turda baglama enjekte edilmez` — dolu kuyrukla `UserPromptSubmit`, `additionalContext` maddeleri de `simdi`yi de taşımıyor |
| 8 | geçti | `node test/run.js` → **440/440** (taban 429, 11 yeni) |
| 9 | geçti | `npx --yes @biomejs/biome check --write` dört js dosyasına koşuldu, güvenli düzeltmeler uygulandı |

### Bilinmesi gerekenler

`Stop` satırı `duyur()` üzerinden gidiyor, yani `steering: 0` (ya da `TEKNESYUM_SESSIZ`)
ayarında yazılmaz. Bilinçli: seviye 0 "hiç `Teknesyum ▸` satırı yazma" demek. O ayardaki
kullanıcı için kuyruk statusline'da ve `/report`'ta duruyor.

`SendMessage` şeması belgelenmemiş; mesaj alanı için `message|text|prompt|content`,
hedef için `agent_id|id|agent|name` sırasıyla bakılıyor. Alan adı bunların dışındaysa
tavan denetlenemez — o durumda `_steered.json` da boş kalır, yani sessiz kalmaz, iz
dosyasının boşluğu belirtiyi verir.

`decision: block` `PostToolUse`'ta mesajı **geri almaz**, gerekçeyi modele döndürür —
mesaj karşı tarafa gitmiştir. Kancanın bağlanabileceği başka nokta yok: `hooks.json`
`PreToolUse` yalnız `Agent|Task` eşliyor ve o dosya `owns` dışında. Sözleşme zaten
`PostToolUse` diyor; not, sonraki turda `PreToolUse` matcher'ı genişletilirse tavanın
gerçekten önleyici olabileceği içindir.

### Kapsam dışı, dokunulmadı

Worktree açılışta `main`'in üç commit gerisindeydi ve sözleşme dosyası worktree'de yoktu;
`git merge --ff-only main` ile eşitlendi, `D1`'in `depo-surum.js`'i de böyle geldi.
Satır `_sorun.log`'da.

`teknesyum/hooks/hooks.json` dosyasına dokunulmadı — gerekmedi: `PostToolUse` matchersız
tanımlı, `SendMessage` zaten kancaya düşüyor; `Stop` da zaten `relay-watch.js`'e bağlı.

## Çıktı

Kesinti artık diskte duruyor, bağlama girmiyor. Üç yeni kanal: `live/_acik.json` (kuyruk,
tavan 10 satır, kanca kırpıyor), `Stop` kancasının tek satırı, statusline'daki
`açıkta N · ajan X/Y`. Ajana giden yönlendirme 5 satırla sınırlı ve tavan `PostToolUse`
üzerinde mekanik: aşan mesaj `decision: block` alıyor, geçen mesaj ajanın kaydına
`steered[]` olarak düşüyor.

Test 429 → **440**, hepsi geçiyor. Biome dört js dosyasına koşuldu.

İki not, ikisi de kapsam dışı bırakıldı:

1. `hooks.json` `PreToolUse` matcher'ı `Agent|Task` ile sınırlı. `SendMessage` oraya da
   eklenirse 5 satır tavanı gerçekten önleyici olur; şu hâlde mesaj gitmiş oluyor ve
   gerekçe modele sonradan dönüyor. Dosya `owns` dışında, karar T0'ın.
2. `statusline.js` röle kökünü worktree eki olmadan çözüyor (`izKoku`), `relay-watch.js`
   ise ajan izlerini `live/worktrees/<ad>/` altına yazıyor. Worktree'de açılan oturumda
   statusline ajanları göremiyor. `_acik.json` bu ayrımın dışında — ana oturumun kuyruğu
   olduğu için ikisi de düz `live/` okuyor. Ayrı bir iş.

---

## Tur 2 — §6 üretimde ölü, kök sebep bulundu

T0 fiilen sınadı: altı satırlık mesaj **engellenmedi**, kısa mesaj da **ize düşmedi**.
440 test geçiyor çünkü testler işlevi doğrudan çağırıyor, gerçek kanca yolunu değil.
`E1`'de de aynı sınıf hata çıkmıştı.

### Kök sebep — ölçüldü

`_hook-debug.log`, gerçek `SendMessage` çağrısı:

```
PostToolUse | SendMessage | session_id=… cwd=… prompt_id=… permission_mode=…
hook_event_name=PostToolUse tool_name=SendMessage tool_use_id=… duration_ms=0
```

**`tool_input` alanı yok.** Kanca mesaj gövdesini hiç görmüyor; `yonlendirmeIzi`
`if (!govde) return;` satırında dönüyor. Ne satır sayılabiliyor ne iz düşüyor.

`_hook-debug.json` alan haritası `PreToolUse`'un `tool_input` **taşıdığını** söylüyor.

İkinci sorun bağımsız: `PostToolUse` zaten engelleyemez, araç çalışmış olur.
`decision: block` orada modele mesaj verir, göndermeyi durdurmaz.

### Yapılacak

`yonlendirmeIzi` `PreToolUse`'a taşınır ve `hooks.json`'daki `PreToolUse` matcher'ına
`SendMessage` eklenir. İkisi birden hem gövdeyi hem engelleme yetkisini verir.

`hooks.json` bu turda `owns`'a eklendi — önceki turda dışarıdaydı ve yapıcı bunu
raporunda doğru biçimde işaret etmişti. Sözleşme hatası T0'a aittir.

### Kabul — bu turda fiilen sınanacak

- Altı satırlık gerçek `SendMessage` **engellenir** (test değil, gerçek çağrı).
- Beş satırlık gerçek mesaj `steered[]`'e **düşer**.
- Hedef alanı `to` üzerinden çözülür — `SendMessage` şemasında parametre adı `to`,
  bugünkü kod `agent_id`/`id`/`agent`/`name` arıyor ve hiçbiri gelmiyor.

Mutasyon disiplini ikisi için de şart.

### Tur 2 raporu

#### Kök sebep düzeltmesi — `tool_input` yok değil, kod yoktu

Sözleşmenin kök sebep okuması iki noktada yanlış; ikisi de ölçüldü.

**1. `PostToolUse` `tool_input` taşıyor.** `_hook-debug.json` alan haritası (4054 olay,
19–24.08.2026):

```
PostToolUse  cwd,duration_ms,effort,hook_event_name,permission_mode,prompt_id,
             session_id,tool_input,tool_name,tool_response,tool_use_id,transcript_path
```

`_hook-debug.log` satırında görünmemesinin sebebi `izSatiri`: yalnız `null`/`string`/
`number`/`boolean` alanları ve yalnız 60 karakteri geçmeyenleri yazıyor. `tool_input`
bir **nesne**, yani o satırda hiçbir araç için hiç görünmez. Log'da alanın yokluğu
alanın yokluğu değildir.

**2. Gerçek sebep: üretimdeki eklentide bu kod hiç yok.** Kurulu sürüm
`~/.claude/plugins/cache/teknesyum/teknesyum/2.42.1` (kurulum 22.08.2026, sha `36d0d0f`).

```
grep -c "yonlendirmeIzi\|SendMessage" <kurulu>/hooks/relay-watch.js   -> 0
```

Tur 1'in kodu worktree'de; `CLAUDE_PLUGIN_ROOT` önbelleğe bakıyor ve projede hook'ları
depoya yönlendiren `.claude/settings.json` yok. T0'ın gerçek çağrısı 2.42.1'i çalıştırdı.
Engellenmemesinin ve ize düşmemesinin sebebi budur; `tool_input` değil.

#### Sözleşmenin haklı olduğu yer

`PostToolUse` **zaten engelleyemez** — araç çalışmıştır. Bu gerekçe tek başına taşımayı
gerektiriyor ve taşındı. Üçüncü kabul maddesi de doğru: hedef alanı `to`.

`SendMessage` şeması (araç tanımından, tahmin değil): `to` (zorunlu), `message` (zorunlu),
`summary`, `notify_when_idle`. Tur 1'in `agent_id|id|agent|name` zinciri hiç eşleşmezdi;
gövde alanı `message` doğruydu.

#### Gerçek çağrı ölçümü — bu turda yapıldı

Canlı bir ajana **altı satırlık gerçek `SendMessage`** atıldı (24.08.2026 10:14:25):

| Ne bakıldı | Sonuç |
|---|---|
| Mesaj engellendi mi | **Hayır** — `success: true`, ajan mesajı aldı ve cevapladı |
| `PostToolUse \| SendMessage` satırı | var (log'da toplam 15) |
| `PreToolUse \| SendMessage` satırı | **0** — beş günlük log'un tamamında sıfır |
| `_steered.json` | yok |

Üçüncü satır bağımsız kanıt: kurulu `hooks.json`'da `PreToolUse` matcher'ı `Agent|Task`
ve `SendMessage` oraya hiç düşmüyor. Matcher genişletilmeden kanca gövdeyi göremez.

Aynı koşu `tool_input` anahtarlarını da ölçtü: `SendMessage:PreToolUse -> message,to`.
Şema ile birebir.

#### Yapılan değişiklik

`teknesyum/hooks/hooks.json` — `PreToolUse` matcher'ı `Agent|Task` → `Agent|Task|SendMessage`.

`teknesyum/hooks/relay-watch.js` — `yonlendirmeIzi` `PostToolUse` dalından `PreToolUse`
dalına taşındı. `PostToolUse` kolu tümden kaldırıldı; ikisi birden dursa geçen mesaj iki
kez ize düşer ve "kayıtlı sayı = atılan sayı" bozulurdu.

Gövde `t.message`, hedef `t.to`. Uydurma yedek alan bırakılmadı — şema ölçüldü.

Engelleme biçimi bu deponun `PreToolUse` kalıbına çevrildi: `decision: block` yerine
**stderr + `process.exit(2)`**. Emsal `contract-guard.js:340` ve `ekran-kapisi.js:170`;
ikisi de üretimde bu yolla engelliyor.

`steeredKayit()` eklendi: `to` bir **ad** olabiliyor, dosya adı ise `agent_id`. Sırayla
ham `to`, ` [ref]` eki kırpılmış hâli, sonra `live/*.json` içinde `agent_id` eşleşmesi
aranıyor. Bulunamazsa kayıt `_steered.json`'a düşüyor — alt çizgi bilinçli, statusline
onu ajan saymıyor.

`iz()` artık `_hook-debug.json` içine `girdi` haritası yazıyor:
`"<araç>:<olay>": "<tool_input anahtarları>"`. Bu turdaki yanlış kök sebep okumasının
tekrarını engelliyor — gövde değil, yalnız anahtar adları.

`multi-session.md` §5.3 `PostToolUse` → `PreToolUse` düzeltildi, "PostToolUse'a bağlanırsa
da ölü doğar" gerekçesiyle.

#### Kabul — tur 2

| Madde | Durum | Kanıt |
|---|---|---|
| Altı satırlık gerçek `SendMessage` engellenir | **koşullu** | Kod ve matcher yerinde, `443/443` yeşil; gerçek çağrı ancak eklenti depodan yeniden kurulunca bu kodu çalıştırır |
| Beş satırlık gerçek mesaj `steered[]`'e düşer | **koşullu** | aynı koşul |
| Hedef `to` üzerinden çözülür | **geçti** | Şemadan ve canlı ölçümden (`message,to`) doğrulandı, `[ref]` eki ve `agent_id` eşleşmesi testli |

İlk iki maddenin "koşullu" olmasının sebebi kod değil dağıtım: **bu oturumun kancaları
2.42.1'den geliyor.** Worktree'deki kod ne yaparsa yapsın canlı çağrıyı etkileyemez.
Gerçek çağrıyla sınama ancak eklenti bu koddan yeniden kurulduktan sonra anlamlıdır —
o adım T0'ın.

Ölçülebilen her şey ölçüldü: gerçek payload'ın anahtarları (`message,to`), gerçek olay
adı (`PostToolUse`, `PreToolUse` yok), gerçek engellenmeme. Kalan tek belirsizlik Claude
Code'un `exit 2`'yi onurlandırması ve o zaten `contract-guard.js` ile üretimde çalışıyor.

#### Test

`node test/run.js` → **443/443**. Tur 1'de 440'tı; üç yeni test:

- `hooks.json PreToolUse matcher SendMessage tasir`
- `hedef [ref] ekiyle ve agent_id uzerinden cozulur`
- `debug izi tool_input anahtarlarini da kaydeder`

Engelleme testi `decision: block` yerine **çıkış kodu 2 + stderr** doğruluyor. Test
yükü uydurulmadı: zarfın anahtar kümesi `_hook-debug.json` alan haritasından,
`tool_input` alanları araç şemasından alındı.

Biome dört js dosyasına koşuldu.

#### T0'a not

Tur 1 raporunda "hooks.json owns dışında" satırı bu turda sözleşmeye alındı. Aynı
sınıftan kalan tek şey dağıtım: kod depoda, üretimde değil. `/scan` ya da sürüm
yükseltme yapılmadan bu özellik kullanıcıda çalışmaz.

## Tur 2 — canlı kanıt eki (T0, 25.08.2026 14:04, eklenti güncellenip yeniden başlatıldıktan sonra)

Denetçinin istediği iki fiili sınama kurulu eklentiyle koşuldu:

1. **Altı satırlık gerçek `SendMessage` engellendi.** Hedef: T8 düzeltme yapıcısı. Kanca
   çıktısı birebir: `ENGELLENDİ: Yönlendirme 6 satır, tavan 5. Beş satıra sığmayan şey
   yönlendirme değildir: değişikliği sözleşme dosyasına yaz, ajana tek satır gönder …`
   Mesaj teslim edilmedi, `steered[]`e girmedi.

2. **Beş satırlık gerçek mesaj geçti ve `steered[]`e düştü.** Aynı hedefe beş satırlık
   yönlendirme kuyruklandı ("Message queued for delivery"), `live/_steered.json` son
   girdisi: `hedef: a9094d1edc98e1330 · t: 2026-08-25 14:04:49 · satir: 5` — metin birebir.

Not: ad çözülemeyen ilk beş satırlık deneme de (`hedef: Fable-T8 düzeltme turu`, 14:04:44)
`steered[]`e yazıldı — kayıt PreToolUse anında düşüyor, teslim sonucuna bakmıyor. Sınır
davranışı olarak denetçiye görünür bırakıldı.
