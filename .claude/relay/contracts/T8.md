---
id: T8
status: submitted
round: 3
owns: [install.ps1, install.sh, scripts/post-install.js, teknesyum/scripts/post-install.js, test/run.js, test/indir-kapilari.js, scripts/checksums.js]
---

# T8 · P0-D — kurulum bir işlem olsun, kullanıcı ayarını silmesin

Kaynak: `TSol/docs/KAPSAMLI-DENETIM-2026-08-25.md` · TB-005 ve TB-006.
Doğrulandı (25.08.2026): `post-install.js:62` bozuk JSON'u yakalayınca yalnız uyarı
listesine ekliyor, `s` boş nesne olarak kalıyor; statusline bloğu `sDegisti`'yi açıyor ve
`:108` dosyanın üstüne `{statusLine: ...}` yazıyor. **Kullanıcının kurtarılabilir bütün
ayarları gidiyor.**

## İki ayrı sorun

**Veri kaybı (TB-006).** Parse hatası bir yazma gerekçesi değil, yazmama gerekçesidir.
`CLAUDE.md` de `:122`'de yedeksiz ve atomik olmayan biçimde yeniden yazılıyor.

**Tedarik zinciri (TB-005).** README `irm .../main/install.ps1 | iex` öneriyor;
`install.ps1:25-27` ve `install.sh:23-26` sonra `post-install.js`'i yine değişebilen
`main` üzerinden indirip Node ile çalıştırıyor. `post-install.js:13-26` yönlendirmeleri
sınırsız izliyor — zaman aşımı, gövde tavanı, alan doğrulama, hash yok. Ayrıca sürüm
kayması üretir: etiketli bir eklenti kuran kullanıcı o anki `main` post-install'ını alır.

## Kabul kriterleri

1. Parse hatasında hiçbir yapılandırma dosyasına yazılmaz; non-zero ya da açık
   "elle müdahale gerekiyor" sonucu döner.
2. Her mutasyondan önce tarih/nonce taşıyan yedek alınır; aynı dizinde temp + rename.
3. Kurulum bir işlem gibi ele alınır: bütün plan doğrulanır, sonra uygulanır; yarıda
   kalırsa geri alınır.
4. Kurulum belgesi sürüm/commit sabitli URL'ye taşınır, yayın varlığında sağlama yayımlanır.
5. Post-install ayrıca indirilmez — kurulan paketin içinden çalıştırılır. Zorunlu
   indirmede HTTPS alan listesi, 3xx tavanı, toplam süre, gövde tavanı, SHA-256 ve
   geçici dosya + atomik rename.
6. CI'da temiz bir HOME üzerinde kurulum/kaldırma/geri alma duman testi.

## Kabul testleri

Bozuk JSON, salt okunur dosya, disk dolu, süreç kesintisi, mevcut özel `statusLine`,
mevcut `CLAUDE.md`/`RULES.md`; yönlendirme döngüsü, cevap vermeyen sunucu, 50 MB gövde,
hash uyuşmazlığı, ağ kesintisi, eski sürüm kurulumu. Kullanıcı verisi bayt bayt korunmalı.

## Tur 2 — denetçi KALDI brifingi (25.08.2026)

**K6 delik (tek kritik).** Kurulum ve geri alma duman testi var, **kaldırma yok** — repo'da
kaldırma betiği de yok. Yapılacak: `teknesyum/scripts/post-install.js`'e `--kaldir` yolu
(ya da ayrı uninstall fonksiyonu): eklentinin settings.json'a koyduğu statusLine bloğunu ve
CLAUDE.md/RULES.md eklerini damgalı yedek alarak geri çıkarır, kullanıcının kendi içeriğine
bayt bayt dokunmaz, aynı temp+rename disipliniyle yazar. Temiz HOME'da kur → kaldır → dosyalar
kurulum öncesi hâle döner duman testi `test/run.js`'e girer (CI'da zaten koşuyor).

**Borçlar.** Toplam süre tavanı: her istek kendi 30 s'ini alıyor, 3 yönlendirmeli zincir
~120 s olabilir (post-install.js:52-56) — indirme başına tek bütçeden düşülecek. Owns listesi
gerçek dosyalarla eşitlendi (bu tur frontmatter'da yapıldı). Release workflow otomasyonu bu
sözleşmenin kapsamı dışında, borç olarak deftere.

Kanıt: `node test/run.js` yeşil + kaldırma duman testinin adı ve çıktısı.

## Kayıt noktası (tur 2)

Tur 2 kapsamındaki iki madde tamamlandı, testler yeşil, biçimlendirme yapıldı.

## Çıktı — tur 2

**Kaldırma yolu.** `teknesyum/scripts/post-install.js` → `--kaldir` argümanı `kaldir()`
fonksiyonunu çağırır. Yalnız eklenti izleri çıkarılır: `teknesyum-statusline` imzalı
statusLine bloğu (kullanıcının kendi statusLine'ı atlanır), dokunulmamış RULES.md şablonu
(kullanıcı kural eklediyse korunur), CLAUDE.md'deki `@RULES.md` satırı (satır aralığı
bozulmadan, kalan içerik bayt bayt korunur). Mevcut `plan/planDenetle/uygula` işlem
disiplini kullanılır; silmeler `cikar()` ile plana girer, her mutasyon damgalı
`.teknesyum-yedek-*` alır, yeniden yazmalar temp+rename, yarıda kalırsa geri alınır.
Bozuk settings.json'da dosyaya dokunulmaz, non-zero döner.

**Toplam süre bütçesi.** `indir()` artık her adımda (ilk istek dahil) kalan bütçeyi
`SURE_TAVANI - (Date.now() - t0)` olarak hesaplar; tükenmişse "toplam süre aşıldı" ile
düşer, tükenmemişse isteğin `timeout`'u kalan bütçedir. 3 yönlendirmeli zincir toplamda
30 s'i aşamaz.

**Testler.** `test/run.js` "Kurulum işlemi" bölümüne 4 test: temiz HOME kur→kaldır iz
kalmaz; kullanıcı dosyalı kur→kaldır bayt bayt geri döner; kullanıcı statusLine/kuralları
korunur; bozuk JSON'da dokunulmaz. `test/indir-kapilari.js`'e 2 denetim: bütçe tükenince
"toplam süre aşıldı"; istek zaman aşımı kalan bütçeden düşer.

Kanıt: `node test/run.js` → `✓ GEÇTİ 532/532` (biome format sonrası tekrar yeşil).
Duman testi adları: "temiz HOME: kur → kaldir sonrasi yedekler disinda iz kalmaz",
"kur → kaldir dongusu kullanici dosyalarini bayt bayt geri getirir".

Not (kapsam dışı, karar T0'ın): kurulumun yazdığı `autoCompactWindow` kaldırmada geri
alınmıyor — hangi değeri kullanıcının koyduğu ayırt edilemiyor; brifing yalnız statusLine
ve CLAUDE.md/RULES.md eklerini istedi.

## Kayıt noktası (tur 3)

Tur 3 tek kritiği tamamlandı, testler yeşil, biçimlendirme yapıldı.

## Çıktı — tur 3

**İz mekanizması.** Kurulum `@RULES.md` satırını gerçekten eklediğinde eklenti durum
dosyası `teknesyum.json`'a `claudeMdRulesEklendi: true` alanı yazar (mevcut alanlar
korunur, yazma plan/uygula disiplininden geçer: damgalı yedek + temp+rename). Satır
zaten varsa iz yazılmaz. `teknesyum.json` bozuksa iz yazılamaz, uyarı düşer, satır
kaldırmada korunur.

**Kaldırma.** `kaldir()` satırı yalnız iz `true` iken çıkarır; iz yokken
"@RULES.md satırını kurulum eklemedi (iz yok), sizin sayıldı ve korundu" der ve dosyaya
dokunmaz — geriye dönük kurulumlar güvenli tarafta. İz tüketilince ya da satır zaten
yokken bayat kalmışsa silinir; `teknesyum.json` yalnız izi taşıyorsa dosya kaldırılır.

**Testler.** `test/run.js` duman bölümüne 2 test:
"kullanicinin eklenti oncesi @RULES.md satiri kur → kaldir dongusunde aynen kalir"
(`@RTK.md` + `@RULES.md` taşıyan gerçek senaryo, bayt bayt) ve
"kurulumun ekledigi @RULES.md satiri kaldirmada cikar, iz teknesyum.json ile tasinir".

Kanıt: `node test/run.js` → `✓ GEÇTİ 555/555` (biome format sonrası tekrar yeşil).

Not: worktree izolasyonu canonical sözleşme yoluna yazmayı engelledi; kayıt ve çıktı
worktree kopyasına işlendi, canonical dosyadaki tur 3 brifingi uygulandı.
