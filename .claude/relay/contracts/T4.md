---
id: T4
status: open
round: 1
owns: [test/run.js, .github/workflows/test.yml, package.json]
---

# T4 · P1 — CI'yi hermetik yap, 71 koştur kırık

Kaynak: `TSol/docs/KAPSAMLI-DENETIM-2026-08-25.md` · TB-008, TB-011, TB-012 ve
`TSol/docs/FIXTURE-ENVANTERI.md` (satır satır fixture envanteri).

Doğrulandı (25.08.2026): son 100 koşunun 80'i başarısız, son başarılı koşu 20.08.2026,
ondan sonra **71 ardışık başarısız**. Yerelde 497/497 geçmesi taşınabilirlik kanıtı değil.

## Kök nedenler — envanterden

| Sınıf | Ne yapılacak | Maliyet |
|---|---|---|
| PATH'te `claude` | `rcCalistir` gerçek PATH'i miras alıyor; sahte binary fixture'ı zaten kısmen var, genelleştir | tek fonksiyon |
| Git kimliği / upstream | `/ozel` üretim subprocess'i kimliği almıyor; ilk push için upstream fixture'ı yok | tek fonksiyon |
| Ses / WAV | `run.js:6501-6855` Windows WAV adlarına ve sürelerine bağlı; sentetik WAV header fixture'ı gerek | tek fonksiyon |
| `maxBuffer` / stdout | 14 wrapper `maxBuffer`, `error`, `signal` kontrolü yapmıyor; macOS'ta `run.js:6458` kesik JSON verdi | yapısal |
| temp sızıntısı | 132 `mkdtempSync` noktası, run-scoped cleanup yok. **Ölçüldü: %TEMP% altında 220.576 dizin** | yapısal |
| 750 UI kontrolü | `u3/u4/u7/u8/u9` ne `npm test` ne CI tarafından çağrılıyor | tek satır |
| lint | 2 gerçek hata: `relay-watch.js` callback return, `test/u4-renk.js:238` kontrol karakteri regex'i | tek satır |

## Kabul kriterleri

Üç OS × en az iki Node LTS, üst üste üç yeşil koşu. Makinede `claude` kurulu olmasa ve
global git config bulunmasa da aynı sonuç. Koşu sonrası çalışma ağacı ve temp kökü temiz.

## Sıra

Önce tek-satır olanlar (UI suite'lerini `npm test`e bağla, iki lint hatası), sonra
tek-fonksiyon fixture'lar, en son yapısal iki madde. Her adım ayrı commit.
