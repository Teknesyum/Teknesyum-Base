---
id: U7
title: Avalonia — Theme.axaml
role: ui-builder
depends: [U2]
owns: [teknesyum/skills/teknesyum-ui/assets/Theme.axaml, teknesyum/skills/teknesyum-ui/assets/Signature.axaml, teknesyum/skills/teknesyum-ui/references/avalonia.md, test/u7-avalonia.js]
side_effects: []
risk: high
status: submitted
round: 1
agent_id: ui-builder
audit: —
auditor_id: —
diff: —
verification: node test/u7-avalonia.js — 17 gecti, 0 kaldi, GEÇTİ
---
## Amaç

`standartlar.md` çok platformda **Avalonia** diyor; arayüz standardımızın Avalonia
karşılığı ise hiç yok. Yani kendi kuralımızı izleyen bir proje arayüz standardımızı
uygulayamıyor. `Theme.xaml` Avalonia'da doğrudan çalışmaz —
`SystemParameters.FocusVisualStyleKey`, `DropShadowEffect` ve trigger sözdizimi farklı.

fable: *"Sessiz boşluk en kötüsü."* Kullanıcı: *"fable iyi tespit etmiş, bunu çözecek
şekilde theme.axaml yazılsın."*

**Bu sözleşme kendi konseyini açar.** Kullanıcı 23.08.2026'da şöyle dedi: *"7 maddeyi
kendi sözleşmesi ve kendi konseyiyle detaylıca inceleyip yap, hepsini abine danış."*
Yani: iş başlamadan önce `fable` ile ayrı bir görüş alınır, çıkan öneri sözleşmenin
`## Konsey

**fable · 23.08.2026.** Görüş bağlayıcı değil ama katılmıyorsan gerekçeni yaz.

> **Birebir çeviri değil, davranış çevirisi yap.** Token adlarını aynen koru; animasyon ve
> tetikleyici katmanını Avalonia'nın kendi mekanizmalarıyla yeniden yaz. **Storyboard
> taklidi kurmaya çalışma.**

### WPF → Avalonia haritası

| WPF | Avalonia | İkame |
|---|---|---|
| `SystemParameters.FocusVisualStyleKey` | **yok** | `Control` için global style + `Setter Property="FocusAdorner"`, tek `FocusAdornerTemplate` |
| `DropShadowEffect` | var | Border'da `BoxShadow="0 0 8 0 #CC00F3FF"` (ucuz); hero metni için `Effect="drop-shadow(...)"` |
| `Style.Triggers` + Enter/ExitActions | **yok** | selector pseudo-class (`Button:pointerover /template/ Border#bd`) + `Transitions` |
| `Storyboard` + `DoubleAnimation` | `Style.Animations` | hover/press için **kullanma**, Transitions yeter; yalnız sonsuz zemin için |
| `ControlTemplate` + `TemplateBinding` | birebir var | `ContentPresenter Name="PART_ContentPresenter"` bekleniyor |
| `StaticResource`/`DynamicResource` | ikisi de var | tek tema, `StaticResource` yeterli |
| `WindowChrome.IsHitTestVisibleInChrome` | **yok** | `ExtendClientAreaToDecorationsHint` + imzayı `IsHitTestVisible="True"` normal öğe olarak koy |
| `LineStackingStrategy` | **yok, gerek de yok** | Avalonia `LineHeight` zaten kutu yüksekliği gibi davranıyor, CSS'e WPF'ten yakın |
| `Typography.NumeralAlignment` | **yok** | `FontFeatures="tnum"` (11.1+); yoksa mono ile karşıla — hangisini seçtiğini şablona yaz |

**Transitions çift yönlüdür**, yani WPF'teki `EIn`/`EOut` ayrımı tek `Easing` değerine
iner. Bu kabul edilebilir bir sadeleşme; **sözleşmeye not düş, sessiz geçme.**

### Token adları

Aynı `x:Key` adları birebir taşınır; **eşleme tablosu gerekmez ve daha kırılgandır.**
`ThemeVariant` kullanma — standart tek (koyu) temadır, `ResourceDictionary` düz kalsın.
Tek istisna: `Duration` yerine `TimeSpan` kaynak, ad aynı (`TInstant` vb.).

### Odak halkası

`FocusAdornerTemplate` içinde `Theme.xaml`'dakiyle aynı iki `Rectangle`: dış
`Stroke="#FF00F3FF" StrokeThickness="2" RadiusX/Y=9 Margin=2`, iç
`Stroke="#FF000000" StrokeThickness="2" RadiusX/Y=7 Margin=4`, sarmalayıcı `Margin="-5"`.
Değerler DIP olarak birebir taşınır.

### Zemin ve azaltılmış hareket

`Style.Animations` → `Animation Duration="0:0:48" IterationCount="Infinite"
PlaybackDirection="Alternate"`, içinde `LinearGradientBrush.EndPoint` keyframe'i
(`RelativePoint` 0.5,1 → 0.7,1).

**Azaltılmış hareket Avalonia'da hazır API'siz.** `SystemParameters.ClientAreaAnimation`
yok; Windows'ta `SPI_GETCLIENTAREAANIMATION` p/invoke gerekir. Şablona somut kural yaz:
animasyon `Classes="anim"` selector'ına bağlanır, uygulama açılışta azaltılmış hareket
tercihini okur ve class'ı düşürür; okuma kodu `references/avalonia.md`'ye örnek olarak
girer. **Böylece kural bu kez şablonda uygulanmış olur** — WPF tarafında kural yazılıydı
ama şablon uygulamıyordu, aynı hataya düşme.

### U1'in bulduğu tuzak

Avalonia'da `Freezable` yok, style mühürleme yok — **o tuzak yok.** Ama akrabası var:
Setter değeri olarak verilen bir transform örneği yine paylaşılabilir. Kaçınma doğal:
`RenderTransform` örneği hiç verme, `ScaleTransition` ya da selector'da
`Setter Property="RenderTransform" Value="scale(0.98)"` kullan — string parse edilir, her
öğeye ayrı üretilir. **Bunu axaml yorumuna yaz ki U1 dersinin karşılığı görünür olsun.**

### Test — Node'dan ölçülebilenler

1. AXAML iyi biçimli XML mi (küçük el yazması kontrol; regex değil).
2. `Theme.xaml`'daki bütün `x:Key` adları `Theme.axaml`'da birebir var mı, fazlalık var mı.
3. Renk değerleri iki dosyada aynı hex mi.
4. Yasak dizgiler: `Style.Triggers`, `LineStackingStrategy`, `DropShadowEffect`,
   `clr-namespace:System.Windows` axaml'de **geçmemeli**.
5. `Signature.axaml` ve `Theme.axaml`'ın kullandığı her `{StaticResource X}` tanımlı mı.
6. Azaltılmış hareket ikamesinin class adı hem axaml'de hem `avalonia.md`'de geçiyor mu.

**Gerçek render ölçülmedi — testin başına bunu yaz.**

### Kapsam

`Theme.xaml`'daki her kuralın karşılığı, o kadar. Avalonia'nın genel kontrol temasını
(TextBox, ComboBox, Fluent override) **yazma**. Fazla büyürse ilk düşecek: Fluent tema
entegrasyon rehberi; ikinci: `FontFeatures` ayrıntısı (mono ikameyle geç).

### En büyük risk

**Storyboard/Trigger yapısını `Style.Animations` içinde birebir taklit etmek.** Hover/press
için keyframe animasyonu yazılırsa Avalonia'da state çıkışında geri sarmaz ve düğme takılı
kalır. Doğru araç **Transitions + pseudo-class setter**.

### fable'ın ayrıca uyardığı — sözleşmenin hiç anmadığı

`Signature.xaml`'daki `Hyperlink` **Avalonia'da yok** ve `loc:Str` markup extension'ının
karşılığı ayrı bir iş. İmza çipleri `Button`/`TextBlock` + `PointerPressed` ya da
`HyperlinkButton` (11.2+) ile kurulmalı, `RequestNavigate` code-behind'i
`Launcher.LaunchUriAsync` (TopLevel) ile değişmeli. **`Signature.axaml`'ın en çok emek
isteyen kısmı budur.**

**Sürüm teyidi sende:** `FontFeatures` ve `HyperlinkButton` sürüme bağlı; hedef Avalonia
sürümüne karşı doğrula, doğrulayamıyorsan `(sürüm teyidi yapılmadı)` diye işaretle.

## Bağlam

- Oku: `teknesyum/skills/teknesyum-ui/assets/Theme.xaml` — çevrilecek kaynak.
- Oku: `teknesyum/skills/relay/references/standartlar.md` §1 — Avalonia kuralı.
- Tespit: U1 denetimi WPF tarafında `ScaleTransform`'un Style Setter değeri olarak
  verilmesinin iki ayrı hataya yol açtığını ölçtü; aynı tuzağa Avalonia'da düşme.
- Oku: `docs/KONSEY-ui-analizi-2026-08-23.md` — bu maddenin ölçümü ve tartışması.
- Oku: `docs/KARARLAR-ui-2026-08-23.md` — alınan karar.
- Kural: **Alternatif sunmadan yasak koyma.** Bir şeyi yasaklıyorsan yerine ne konacağını
  da yaz; kurallar tavsiye niteliğinde alınmıştı, kesin kural gibi bela olmayacaklar.
- Kural: **Çelişki bulursan kendin çözme, kullanıcıya sor.** Depoda genel bir öncelik
  kuralı yok ve bilerek yok.
- Kural: Ölçmediğin sayıyı ölçmüş gibi yazma; `(varsayılan, ölçülmedi)` etiketi serbest.

## Kayıt noktası

Dört dosya da yazıldı, `node test/u7-avalonia.js` tek başına koşuyor ve GEÇTİ veriyor
(17 geçti, 0 kaldı). Açık iş kalmadı, denetime hazır.

## Çıktı

### Ne yazıldı

- `teknesyum/skills/teknesyum-ui/assets/Theme.axaml` — `Theme.xaml`'ın Avalonia 11
  karşılığı. Kök `Styles` (kaynak + genel kural aynı dosyada), token adları birebir aynı.
- `teknesyum/skills/teknesyum-ui/assets/Signature.axaml` — imza bloğu, `Hyperlink` yerine
  `Button`.
- `teknesyum/skills/teknesyum-ui/references/avalonia.md` — 13 bölüm; WPF'i bilene farkı
  anlatır, çözülemeyen üç işi saklamaz.
- `test/u7-avalonia.js` — tek başına koşan 17 ölçüm.

### Konseyden sapmalar — gerekçeleriyle

**1. `DropShadowEffect` yasak dizgi listesinden çıkarıldı.** Konsey bu adı `axaml`'de
geçmemesi gereken dizgiler arasına koymuştu; gerekçesi WPF artığını yakalamaktı. Avalonia
11'de `DropShadowEffect` gerçek bir API ve **metne glow veren tek yol**. `HeroGlow`
token'ı bu yüzden bir `DropShadowEffect` kaynağı olarak duruyor. Yerine test WPF'e özgü ve
Avalonia'da hiç bulunmayan `ShadowDepth` dizgisini arıyor — WPF artığı yine yakalanıyor,
çalışan API yasaklanmıyor. Kutu glow'u konseyin dediği gibi `BoxShadow`.

**2. `EOut` / `EIn` `CubicEase` değil `SplineEasing`.** SKILL §5.4 token'ı
`cubic-bezier(0.2, 0, 0, 1)`; `SplineEasing` bu eğriyi birebir taşıyor, `CubicEaseOut`
taşımıyor. WPF sürümü `CubicEase` kullanıyor çünkü WPF'te spline karşılığı yok — yani bu
sapma Avalonia'yı WPF'e değil **CSS'e** yaklaştırıyor.

**3. Zemin dönüşü `EndPoint` keyframe'i değil katman dönüşü.** Konsey
`LinearGradientBrush.EndPoint` keyframe'i öneriyordu. Avalonia bir fırçanın alt özelliğini
enterpole **edemez** (fırça animatörü yalnız düz rengi taşır) ve fırça bir `StyledElement`
olmadığı için selector'la da hedeflenemez. Aynı görünüm, gradientle boyalı boş katmanın
`RenderTransform`'unun çok yavaş dönmesiyle üretildi. Açı hesabı: `atan(0.5)=26.57°`,
`atan(0.6)=30.96°`, `atan(0.7)=34.99°` → duruk değere göre `-4.39°` ve `+4.03°`, toplam
`8.42°`, `motion.md` M10'un 20° tavanının altında. Ölçek `1.12` (gereken en küçük `1.09`
hesaplandı, pay bırakıldı). **Bütün bu sayılar hesaplandı, ekranda ölçülmedi.**

**4. Azaltılmış hareket sınıfı `Panel`'de değil `Window`'da.** Konsey `Classes="anim"`
dedi, yeri demedi. Sınıf pencerede tek yerde duruyor, kurallar ona bakıyor
(`Window.anim Panel.appbg`, `Window:not(.anim) …`). Alt öğelere dağıtılsaydı bir tanesi
unutulduğunda ayar yarım uygulanırdı.

### Kabul edilen sadeleşmeler — sessiz geçilmiyor

- **`Transition` tek `Easing` alır.** WPF'in `EIn` (çıkarken) ve `EOut` (girerken) ayrımı
  Avalonia'da yapılamıyor; iki yönde de `EOut` kullanıldı. `EIn` token'ı tanımlı duruyor
  ama şablonda kullanılmıyor.
- **`ColorInterpolationMode` yok.** WPF `ScRgbLinearInterpolation` karşılıksız, sRGB
  zorunlu. 11 durak bantlaşmayı zaten kapatıyor; görünür fark olup olmadığı ölçülmedi.
- **`x:Shared` yok.** Fırça tek örnek paylaşılıyor. Bu şablonda sorun çıkarmıyor çünkü
  animasyon fırçaya değil katmana uygulanıyor; fırçayı kod içinden değiştiren proje bütün
  pencereleri etkiler, bu `avalonia.md` §5'te yazılı.

### Sürüm teyidi yapılmadı — dördü de

Hedef Avalonia sürümüne karşı hiçbir doğrulama yapılamadı, `dotnet` derlemesi koşulmadı:

- **`FontFeatures="+tnum"` (11.1+)** — `Typography.NumeralAlignment` karşılığı. Şablonda
  **kapalı** bırakıldı: özellik yoksa XAML hiç yüklenmez ve tema tümden düşer. Seçilen
  ikame **mono** (SKILL §3, "her sayı mono"); açma satırı `Theme.axaml`'da yorumda hazır.
- **`TopLevel.Launcher` (11.1+)** — kullanıldı, eski sürüm karşılığı `Process.Start`
  `Signature.axaml`'ın code-behind bloğunda yazılı.
- **`HyperlinkButton` (11.2+)** — **kullanılmadı.** Doğrulayamadığım bir sürüm tabanını
  şablonu kopyalayan her projeye dayatmamak için; geçiş yolu `avalonia.md` §6'da.
- **`SplineEasing`** — kullanıldı, `CubicEaseOut` / `CubicEaseIn` düşüş yolu
  `Theme.axaml` yorumunda.

### Çözülemeyen iki iş — uydurulmadı, yazıldı

**1. `loc:Str`'ın Avalonia karşılığı yarım.** `System.Windows.Markup.MarkupExtension` yok;
`ProvideValue` metodu olan düz bir sınıf yeter ve iskeleti `avalonia.md` §6'da. Ama değer
**bir kez** çözülüyor: `desktop.md` §9 "dil seçimi anında uygulanır, yeniden başlatma
istemez" diyor, düz `string` döndüren extension bunu sağlamıyor. Anlık güncelleme için
`ProvideValue`'nun bir `IBinding` döndürmesi ve sözlüğün dil değişince değişiklik yayması
gerekir. **Şablon o bağlamayı içermiyor**; `Signature.axaml` derlenebilir bir `Str` sınıfı
bekliyor ve kendisi sağlamıyor.

**2. Azaltılmış hareket macOS ve Linux'ta okunmuyor.** Windows yolu
(`SPI_GETCLIENTAREAANIMATION`) `avalonia.md` §4'te tam kodla var. macOS karşılığı
`NSWorkspace.accessibilityDisplayShouldReduceMotion`, Avalonia'dan doğrudan görünmüyor;
Linux'ta tek bir kaynak yok. O platformlarda tercih okunmuyor ve kullanıcıya arayüz
içinden bir anahtar sunulması gerekiyor — bu `avalonia.md`'de açıkça yazılı.

### Ölçülmeyen — testin ölçmediği

`test/u7-avalonia.js` **gerçek render ölçmüyor** ve bunu ilk satırında yazıyor. Node'dan
Avalonia çalıştırılamıyor. Ölçülen: XML iyi biçimliliği, token adı eşitliği, renk hex
eşitliği, WPF artığı yokluğu, kaynak tanımlılığı, azaltılmış hareket ikamesinin hem
şablonda hem belgede oluşu. **Ölçülmeyen:** şablonun derlendiği, halkanın göründüğü,
zeminin döndüğü, çipin doğru yerde durduğu. Bunlar `desktop.md` §11'in ekran görüntüsü
yöntemiyle elle doğrulanır ve **henüz yapılmadı**.

### `SKILL.md`'ye girmesi gereken metin — U10 buradan alsın

`SKILL.md` benim değil. Aşağıdaki üç parça birebir yazılacak metindir.

**(a) §1 kurulum tablosuna, `WPF / .NET` satırının hemen altına yeni satır:**

    | Avalonia | `assets/Theme.axaml` | `Assets/Theme.axaml`, `App.axaml` içinde `StyleInclude` |

**(b) §4'ün "Hazır bileşen" satırı. Mevcut satır:**

    Hazır bileşen: `assets/Signature.tsx` (React) · `assets/Signature.xaml` (WPF).

**yerine:**

    Hazır bileşen: `assets/Signature.tsx` (React) · `assets/Signature.xaml` (WPF) ·
    `assets/Signature.axaml` (Avalonia).

**(c) §7'nin sonuna yeni paragraf:**

    **Avalonia işinde `references/avalonia.md` zorunlu.** `assets/Theme.xaml` orada
    çalışmaz: `SystemParameters.FocusVisualStyleKey`, `Style.Triggers`, `Storyboard` ve
    `LineStackingStrategy` Avalonia'da yok. Karşılığı `assets/Theme.axaml` ve
    `assets/Signature.axaml`; token adları birebir aynı, değişen yalnız mekanizma.
    `references/desktop.md` ve `references/motion.md` Avalonia'da da yürürlüktedir —
    kural değişmedi, aracı değişti.

### Kullanıcıya sorulacak — kendim çözmedim

**Token adları kaç dosyada yaşayacak?** `Theme.axaml` aynı anahtarları ve aynı hex'leri
`Theme.xaml`, `theme.css`, `Palette.cs` ve `SKILL.md` §3'ten sonra **beşinci** kez
taşıyor. `test/u7-avalonia.js` yalnız `Theme.xaml` ile `Theme.axaml` arasındaki sapmayı
yakalıyor. Beş kopyayı tek denetimde buluşturmak U9'un işi mi, ayrı bir madde mi — depoda
genel bir öncelik kuralı yok ve bilerek yok, bu yüzden kendim karar vermedim.

### CHECK: çıktısı

Sözleşmede `CHECK:` satırı ve `## Kabul` bölümü **yok** (bkz. `## Rapor`). Konseyin
"Test — Node'dan ölçülebilenler" başlığındaki altı madde kabul kriteri olarak alındı;
test bu altı maddeye göre numaralandırıldı.

    $ node test/u7-avalonia.js
    U7 — Avalonia sablonu (gercek render olculmedi)

      ✓ 1 · Theme.axaml iyi bicimli XML
      ✓ 1 · Signature.axaml iyi bicimli XML
      ✓ 1 · Theme.xaml (kaynak) iyi bicimli XML
      ✓ 2 · WPF anahtarlarinin hepsi Avalonia surumunde var
      ✓ 2 · Avalonia surumunde fazladan anahtar yok
      ✓ 2 · karsiligi olmayan anahtar listesi tam olarak beklenen
      ✓ 3 · ortak anahtarlarin renk degerleri iki dosyada ayni
      ✓ 3 · zemin gradientinin 11 duragi birebir ayni
      ✓ 4 · Theme.axaml WPF artigi tasimiyor
      ✓ 4 · Signature.axaml WPF artigi tasimiyor
      ✓ 4 · hover ve basma keyframe degil Transition ile yazilmis
      ✓ 4 · RenderTransform hicbir yerde nesne olarak verilmiyor (U1 dersi)
      ✓ 5 · kullanilan her StaticResource tanimli
      ✓ 5 · Signature.axaml kendi iki temasini tanimliyor
      ✓ 6 · azaltilmis hareket sinifi sablonda uygulanmis
      ✓ 6 · azaltilmis hareket sinifi ve okuma kodu belgede
      ✓ 6 · belge cozemedigi isleri sakli tutmuyor

    17 gecti, 0 kaldi
    GEÇTİ

`test/run.js`'e dokunulmadı, `SKILL.md` ve `Theme.xaml` açılmadı bile — yalnız okundu.

## Rapor

- Sözleşmede `## Kabul` bölümü ve tek bir `CHECK:` satırı yok; `## Amaç` da
  "sözleşmenin `## Konsey`" cümlesinin ortasında kesilmiş. Konseyin altı ölçüm maddesi
  kabul kriteri olarak alındı ve test onlara göre yazıldı.
- Çevrilen kaynak dosya iş sırasında altımdan değişti. `assets/Theme.xaml`'ı okuduğumda
  273 satırdı; `Theme.axaml` yazılırken 321 satır oldu — U4'ün anlamsal rol katmanı
  (`Danger`, `DangerText`, `Warning`, `Warning50`, `WarningPanel`, `WarningBody`,
  `DangerBody` ve `NeonSuccess` → `Success`) o sırada indi. Testin anahtar eşitliği
  ölçümü sapmayı yakaladı, `Theme.axaml` yeni sürüme göre tamamlandı. Ders: çeviri
  sözleşmesinde kaynağı bir kez okuyup geçmek yetmiyor, eşitliği ölçen bir test şart.
- Bu sözleşme `submitted` olduktan sonra çalışma ağacı dışarıdan sıfırlandı (`git stash`),
  `U7.md` bir süre HEAD sürümüne döndü ve test 2 maddede kaldı. Stash sonradan geri
  alındı, dosya ve test eski hâline döndü — `git`'e dokunmadım. `owns` altındaki dört
  dosya untracked olduğu için stash'a hiç girmedi ve diskte kaldı.
