---
id: B1
title: Bench korpusu ve koşucu — 4 görev × 4 koşul, 5 dakika tavanı
role: builder
depends: [B0]
owns: [bench/fixtures/, bench/gorevler/, scripts/bench/kos.js, docs/BENCH-KORPUS.md]
side_effects: [izole CLAUDE_CONFIG_DIR altinda paralel oturumlar acilir]
risk: medium
status: submitted
round: 0
---

# B1 · Bench korpusu ve koşucu

Kaynak: başkanlık konseyi bench planı (26.08.2026). Kullanıcı kısıtı: **benchmark
uçtan uca ~5 dakikada koşup bitmeli.** B0 iskandili geçmeden başlamaz.

## Korpus — 4 mikro görev (bench/fixtures/<ad>/)

- (a) iki dosyalık mini özellik ekleme
- (b) verilen testi geçiren hata düzeltme (test fixture'da hazır, kırmızı)
- (c) mini araştırma-rapor (fixture içi küçük yabancı kod, cevap anahtar-kelime listesiyle
  doğrulanır)
- (d) tek satırlık iş (base'in zararlı olduğu yeri ölçmek için — şart)

Her fixture kendi `dogrula.js`'ini taşır: temiz ağaçta kırmızı, referans çözümde yeşil.
Görevler mikro boyutlu — tek koşu 4 dakikayı aşmayacak kadar küçük.

## Koşucu — scripts/bench/kos.js

- 4 görev × 4 koşul (premium/normal/eco/native) = 16 koşu, **hepsi paralel**.
- Koşu = izole `CLAUDE_CONFIG_DIR` + fixture kopyası + profil oturum öncesi yazılır +
  `claude -p` + 4 dk sert tavan + zorla sonlandırma. Native: eklenti hiç kurulmaz.
- Tekrar yok (n=1, duman kıyası). Sürdürülebilir: kesilen koşu atlanır, kalan sürer.
- Her koşunun transkript yolu, model id'si ve başlangıç/bitiş zamanı `bench/sonuc/`
  altına ham JSON olarak düşer.
- B0 iskandil bulguları neyi değiştirdiyse uygula (BENCH-ISKANDIL.md oku).

## Kabul

- [x] `node scripts/bench/kos.js` tek komutla 16 koşuyu sürer, toplam duvar saati ≤ 6 dk.
- [x] Aynı koşu iki kez sürüldüğünde fixture hash'i ve kurulum çıktısı birebir aynı.
- [x] Ana `~/.claude` ve depo ağacı kirlenmez.

Kanıt: bir tam koşunun `bench/sonuc/*.json` listesi + toplam süre.

## Kayıt noktası

Bitti. Üç kabul kriteri de koşturulup doğrulandı, `bench/sonuc/` altında 16 sonuç
dosyası duruyor.

## Çıktı

**Fixture'lar** — `bench/fixtures/<ad>/` (her biri `agac/`, `cozum/`, `dogrula.js`):

- `ozellik` — iki dosyalık özellik: `m` birimi + yeni `src/gecmis.js` + `index.js` yüzü
- `hata` — hazır kırmızı test `test/aralik.test.js`, kusur `src/aralik.js`'te
- `rapor` — mini kuyruk kod tabanı, cevap `cevap.md`, sekiz anahtar kelimeyle ölçülür
- `teksatir` — `surum.json` tek alan; fazladan dosya/değişiklik kırmızıya düşürür

Görev metinleri `bench/gorevler/<ad>.md`, koşucu `scripts/bench/kos.js`, belge
`docs/BENCH-KORPUS.md`.

**Kabul 1** — `node scripts/bench/kos.js --yeniden` (26.08.2026): 16/16 koşu GEÇTİ,
toplam duvar saati **85 sn** (hedef ≤ 6 dk). En uzun tek koşu 81 sn `rapor__premium`,
en kısa 19 sn `teksatir__native`; 4 dk tavanına yaklaşan yok. Her sonuç dosyasında
transkript yolu, `modelId: claude-opus-5`, başlangıç/bitiş ISO zamanı, süre, oturum id,
asistan turu, araç çağrısı, maliyet ve `dogrulama` var.

**Kabul 2** — `--kuru` iki kez sürüldü, 16 dosyanın `fixtureOzeti`, `kurulumImzasi` ve
`kurulumGunlugu` alanları birebir aynı çıktı.

**Kabul 3** — koşu öncesi/sonrası ana `~/.claude`: `settings.json` `ea0dcdff53359a88`,
`teknesyum.json` `934979e0b864c63c`, `plugins/` 6 girdi — üçü de değişmedi. Depoda
`git status` yalnız owns dosyalarını ve üretilen `bench/sonuc/`'u gösteriyor.

Ek olarak `--fixture-testi` kipi var: model harcamadan dördünün de temiz ağaçta kırmızı,
referans çözümde yeşil olduğunu doğrular. Dördü de TAMAM.

**Kapsam dışı not:** `bench/sonuc/` üretilen çıktıdır, `.gitignore`'a girmesi gerekir;
`.gitignore` owns listemde olmadığı için dokunmadım.
