---
id: T3
status: submitted
round: 2
owns: [teknesyum/hooks/contract-schema.js, teknesyum/hooks/contract-guard.js, teknesyum/hooks/relay-watch.js, test/run.js]
---

# T3 · P0-B — sözleşme kimliği ve durumu tek şemaya iner

Kaynak: `TSol/docs/KAPSAMLI-DENETIM-2026-08-25.md` · TB-003. Bulgu bağımsız doğrulandı
(25.08.2026): `contract-guard.js` içindeki `canonical()` son satırında
`/^T[^/\]+\.md$/i` var. Depoda 22 açık sözleşme dosyası D1-D4, E1, F1-F3, S1, S2, T1,
U1-U11 kimlikleriyle duruyor. Yani **21 sözleşme korumanın tamamen dışında** — `done/`
kapısı, mühür denetimi ve durum gerilemesi onlar için hiç çalışmıyor.

Bu, röle güvenliğinin kendisidir: statusline "23 iş" derken guard yalnız birini görüyor.

## Kabul kriterleri

1. Yeni `teknesyum/hooks/contract-schema.js` tek kaynak: kimlik regex'i, durum enum'u,
   izinli geçiş tablosu, `canonical()` ve `durum()` buradan gelir.
2. Kimlik şeması `^[A-Z][A-Z0-9]*\d*\.md$` gibi mevcut veriyi kapsayan bir biçim olur;
   migrasyon gerekmez. Dar biçim seçilirse 21 dosya taşınır ve pointer'lar güncellenir.
3. `contract-guard.js` kendi regex'ini tutmaz, şemayı içe alır.
4. Bilinmeyen kimlik ya da durum sessizce yok sayılmaz.
5. Test: D1, E1, S2, U7 için `done/` taşıma ve durum gerilemesi engellenir; T1 için
   davranış değişmez. Fixture sözleşme adlarını parametre alır.

## Yapılmayacak

`relay-watch.js:1164` çevresindeki `T*.md` beklentisi bu sözleşmede değil — o T4'ün
kapsamında. Bir turda iki dosya sahipliği çakışmasın.

## Tur 2 — denetçi KALDI brifingi (25.08.2026)

Denetçi iki kritik delik buldu; tur yalnız bunları ve iki borcu kapatır.

**K4 delik.** `contract-guard.js:154` ve `:161` — `if (!bilinenDurum(x)) return;` bilinmeyen
durumu sessizce geçiriyor. İki adımlık yıkama: `status: bozuk` Write → sessiz geçer, sonra
`bozuk → open` Write → eski durum bilinmediği için gerileme denetimi hiç çalışmaz. Düzeltme:
bilinmeyen **yeni** durum engellenir (fail-closed, dil.js'e mesaj); bilinmeyen **eski** durum
da engellenir ya da en azından `_sorun.log`'a yazılıp geçiş gerileme sayılır. Sessiz `return`
kalmayacak.

**K5 delik.** `test/run.js:2024` `sozlesmeProje` fixture'ı `T1.md` adını sabitliyor. Ad
parametre olacak; D1, E1, S2, U7 için durum gerilemesi ve done/ taşıma engel testleri
eklenecek (done/ kapısı listesinde U11 var, U7 yok — kriter U7 diyor, U7 eklenecek).

**Borçlar.** `sealed → open` gerilemesinin engellendiğine test yok — eklenecek.
`relay-watch.js:1183` bilinmeyen durumlu sözleşmeyi listeden sessizce düşürüyor —
`_sorun.log`'a bir satır düşülecek.

Kanıt: `node test/run.js` yeşil + K4 yıkama senaryosunun iki adımı da engellendiğini
gösteren test çıktısı.

## Kayıt noktası

Tur 2 tamamlandı, dört brifing maddesi de kapalı. Worktree: `.claude/worktrees/agent-a85746359c5c61ab4`, dal `t3-tur2`.

## Çıktı

- K4: `contract-schema.js` — `DURUMLAR` kümesi (merdiven + blocked) eklendi, `bilinenDurum` artık bu kümeye bakar. `contract-guard.js` `gerileme()` — bilinmeyen yeni durum fail-closed engellenir; bilinmeyen eski durum `_sorun.log`'a yazılır ve gerileme sayılıp engellenir; status satırı olmayan yazımlar (`yeni === null`) eskisi gibi durum denetimi dışında.
- K4 mesajları dil.js'e değil `contract-guard.js` içindeki yerel `DURUM_ENGELI` tablosuna yazıldı (ECO_ATLAMA kalıbı) — dil.js owns kümesinde yok, dokunulmadı. dil.js'e taşınması istenirse T0 kararı.
- K5: `test/run.js` `sozlesmeProje(eskiDurum, ad)` ad parametresi aldı. Yeni testler: `gerileme engeli T dışı kimliklerde de çalışır` (D1, E1, S2, U7), `T dışı kimlikler kabuktan done/ altına taşınamaz` (D1, E1, S2, U7 mv), `yıkama: bilinmeyen durum iki adımda da engellenir` (K4 kanıtı, iki adım da exit 2). done/ Edit kapısı listesine `U7.md` eklendi.
- Borç 1: `mühürlü sözleşme geri açılamaz: sealed -> open engellenir` testi eklendi.
- Borç 2: `relay-watch.js` `acikSozlesmeler` — bilinmeyen durumlu sözleşme artık sessiz düşmez, `_sorun.log`'a satır bırakır (bilinen ama açık olmayan durumlar sessiz kalır).
- Kanıt: `node test/run.js` → `✓ GEÇTİ 532/532`; `✓ yıkama: bilinmeyen durum iki adımda da engellenir`. Biome format yalnız dört owns dosyasına uygulandı.

## Rapor

- Tur 1 worktree'si kota kesintisi sırasında silinmişti; aynı yola `main`'den `t3-tur2` dalıyla yeni worktree açıldı (tur 1 zaten main'e merge edilmiş, 7441316). `_sorun.log`'a yazıldı.
