---
id: U2
title: Tipografi ve hızlı düzeltmeler dalgası
role: ui-builder
depends: [U1]
owns: [teknesyum/skills/teknesyum-ui/references/components.md, teknesyum/skills/teknesyum-ui/SKILL.md, teknesyum/skills/teknesyum-ui/references/layout.md, teknesyum/skills/teknesyum-ui/references/desktop.md, teknesyum/skills/teknesyum-ui/references/motion.md, teknesyum/skills/teknesyum-ui/assets/theme.css, teknesyum/skills/teknesyum-ui/assets/Theme.xaml, teknesyum/skills/teknesyum-ui/assets/Palette.cs, teknesyum/skills/teknesyum-ui/assets/Signature.tsx, teknesyum/skills/teknesyum-ui/assets/Signature.xaml, test/run.js]
side_effects: []
risk: high
status: submitted
round: 3
agent_id: —
audit: —
auditor_id: —
diff: —
verification: —
---
## Amaç

Konsey raporunun **hemen yapılmalı** listesindeki 16 maddeyi tek dalgada uygular.
Hepsi ucuz, risksiz ve geri dönüşü kolay; ayrı sözleşmelere bölünemezler çünkü üçü de
aynı üç dosyaya (`SKILL.md`, `theme.css`, `Theme.xaml`) dokunuyor ve `owns` çakışırdı.

Kararların kaynağı `docs/KARARLAR-ui-2026-08-23.md`; tartışma `docs/KONSEY-ui-analizi-2026-08-23.md`.
**Karar alınmıştır, yeniden tartışma.** Bir madde uygulanamıyorsa dur ve söyle.

## Kabul kriteri

- [x] K1: Font ailesi Atkinson Hyperlegible Next varsayılan, koşullu değil. Mono'da
      `Cascadia Mono` başta, `Consolas` geride. `SKILL §3`, `theme.css`, `Theme.xaml`
      üçünde de aynı zincir.
      CHECK: node -e "const s=require('fs').readFileSync('teknesyum/skills/teknesyum-ui/assets/theme.css','utf8');process.exit(/Atkinson Hyperlegible Next/.test(s)&&/Cascadia Mono,\s*Consolas/.test(s)?0:1)"
- [x] K2: Ölçek 1.25 oranında beş basamak: `--tk-fs-1: 14px` · `16` · `20` · `24` · `30`.
      Hero 30, h1 24. Eski dört basamaklı ölçekten kalan sabit `px` değeri kalmaz.
      CHECK: node -e "const s=require('fs').readFileSync('teknesyum/skills/teknesyum-ui/assets/theme.css','utf8');process.exit(['--tk-fs-1: 14px','--tk-fs-2: 16px','--tk-fs-3: 20px','--tk-fs-4: 24px','--tk-fs-5: 30px'].every(t=>s.includes(t))?0:1)"
- [x] K3: Başlık ve etiket ağırlığı **600**, hero 900. `700` yalnız hero dışı hiçbir
      tipografi sınıfında kalmaz.
      CHECK: node -e "const s=require('fs').readFileSync('teknesyum/skills/teknesyum-ui/assets/theme.css','utf8');const b=s.slice(s.indexOf('--- tipografi'),s.indexOf('--- yüzeyler'));process.exit(/font-weight:\s*700/.test(b)?1:0)"
- [x] K4: `--tk-lh-body: 1.5` · `--tk-lh-heading: 1.2` · `--tk-lh-mono: 1.4` ·
      `--tk-measure: 65ch` tanımlı ve kullanılıyor. WPF karşılığı `LineHeight` +
      `LineStackingStrategy="BlockLineHeight"`.
      CHECK: node -e "const c=require('fs').readFileSync('teknesyum/skills/teknesyum-ui/assets/theme.css','utf8'),x=require('fs').readFileSync('teknesyum/skills/teknesyum-ui/assets/Theme.xaml','utf8');process.exit(['--tk-lh-body: 1.5','--tk-lh-heading: 1.2','--tk-lh-mono: 1.4','--tk-measure: 65ch'].every(t=>c.includes(t))&&/LineStackingStrategy/.test(x)?0:1)"
- [x] K5: Harf aralığı boyutla ters orantılı: etiket `0.15em`, h3 `0.05em`, h2 `0.02em`,
      h1/hero `0` ya da `-0.01em`. WPF'te tracking uygulanamıyorsa telafi `SKILL §3`'e
      yazılır — sessiz bırakılmaz.
- [x] K6: Başlık hiyerarşisi gözle ayrışıyor: h2 24, h3 18 ya da 20, etiket 14. Üç seviye
      artık aynı renk + aynı ağırlık + aynı tracking taşımıyor.
- [x] K7: `--tk-text-dim` üç dosyadan da **silindi** (`theme.css`, `Theme.xaml`,
      `Palette.cs`). Disabled için tooltip zorunluluğu asset yorumuna yazıldı.
      CHECK: node -e "const g=p=>require('fs').readFileSync(p,'utf8');process.exit(['teknesyum/skills/teknesyum-ui/assets/theme.css','teknesyum/skills/teknesyum-ui/assets/Theme.xaml','teknesyum/skills/teknesyum-ui/assets/Palette.cs'].some(p=>/text-dim|TextDim/i.test(g(p)))?1:0)"
- [x] K8: `--tk-glow-hero` tokenı eklendi; CSS `drop-shadow` ve XAML `DropShadowEffect`
      aynı yoğunluğu veriyor (blur 8, opaklık 0.8).
- [x] K9: Gövdeye `font-variant-numeric: tabular-nums`, WPF'e
      `Typography.NumeralAlignment="Tabular"`. `SKILL §3`'e ayrım yazıldı: **veri sayısı
      mono, cümle içi sayı sans+tabular.**
      CHECK: node -e "const c=require('fs').readFileSync('teknesyum/skills/teknesyum-ui/assets/theme.css','utf8');process.exit(/font-variant-numeric:\s*tabular-nums/.test(c)?0:1)"
- [x] K10: XAML'a `Hint` stili eklendi; `Roboto` tek kaynağa çekildi (ya `SKILL §3`'e
      yazıldı ya CSS'ten çıkarıldı).
- [x] K11: **Yarıçap çelişkisi `layout.md` lehine kapatıldı.** Yuvarlatılmış dikdörtgen
      daha küçük köşe alır; `SKILL §5`'in 16/12/8/6 merdiveni `layout.md`'nin verdiği
      değere çekilir. Kullanıcı kararı, tartışma yok.
- [x] K12: Pencere düğmesi çelişkisi kapatıldı — `SKILL:444` 42×30 ile `desktop:286`
      52×36 tek değere iner. **Hangi değerin doğru olduğunu ölçemiyorsan seçme, sor.**
- [x] K13: `SKILL §2`'ye tek cümle: renk tek başına anlam taşımaz (WCAG 1.4.1). Alt bilgi
      durum noktasına şekil farkı (dolu/halka) eklendi.
- [x] K14: Ölçülmemiş yedi sayıya `(varsayılan, ölçülmedi)` etiketi kondu: 11 durak ·
      döngü ≥40s / açı ≤20° · skeleton ≥1.4s · "Türkçe %20-30 uzun" · kenar çubuğu
      240/48 DIP · pencere düğmesi ölçüsü · yarıçap merdiveni.
      CHECK: node -e "const s=require('fs').readFileSync('teknesyum/skills/teknesyum-ui/SKILL.md','utf8');process.exit((s.match(/\(varsayılan, ölçülmedi\)/g)||[]).length>=5?0:1)"
- [x] K15: `SKILL §0`'a tek cümle: **bu standart yalnız karanlıktır**; aydınlık istenirse
      ayrı palet işidir ve ileriye bırakılmıştır.
      CHECK: node -e "const s=require('fs').readFileSync('teknesyum/skills/teknesyum-ui/SKILL.md','utf8');process.exit(/yalnız karanlık/i.test(s)?0:1)"
- [x] K16: `§5.4`'ün gerekçe paragrafları `references/motion.md`'ye taşındı; `SKILL`'de
      tablo ve tokenlar kaldı. **Şart:** taşınan kural gözden kaçmayacak — `SKILL`'de
      kalan tabloda her satır `motion.md`'nin ilgili başlığına atıf verir ve `§5.4`'ün
      girişinde "hareket işi yapmadan önce `references/motion.md` okunur" cümlesi durur.
- [x] K17: `SKILL.md:416`'daki `NeonFocusVisual` adı düzeltildi — o ad depoda yok.
- [x] K18: Bütün testler geçiyor ve her yeni davranışın testi var.
      CHECK: node test/run.js
      EXPECT: GEÇTİ

## Bağlam

- Oku: `docs/KARARLAR-ui-2026-08-23.md` — kararların tamamı, gerekçeleriyle.
- Oku: `docs/KONSEY-ui-analizi-2026-08-23.md` — her maddenin ölçümü ve tartışması.
- Oku: `teknesyum/skills/teknesyum-ui/SKILL.md` §2 §3 §5 §5.3 §5.4.
- Tespit: `theme.css`, `Theme.xaml` ve `SKILL §3` tablosu **üç ayrı elle yazılmış
  kopyadır.** Üçünü de güncellemezsen ayrışırlar; fable bunu ayrıca uyardı.
- Kural: **Alternatif sunmadan yasak koyma.** Bu kurallar tavsiye niteliğinde alınmıştı;
  kesin kural gibi bela olmayacaklar. Bir şeyi yasaklıyorsan yerine ne konacağını da yaz.
- Kural: **Çelişki bulursan kendin çözme, kullanıcıya sor.** Depoda genel bir öncelik
  kuralı yok ve bilerek yok — "SKILL kazanır" gibi bir kestirme reddedildi.
- Kural: Ölçmediğin sayıyı ölçmüş gibi yazma. `(varsayılan, ölçülmedi)` etiketi serbest.

## Kayıt noktası
Onsekiz maddenin onyedisi uygulandı, K12 bilerek açık bırakıldı. `theme.css` baştan yazıldı:
font zinciri, beş basamaklı ölçek, 600 ağırlık, satır yüksekliği/measure, tracking, tek
yarıçap (`--tk-r: 6px`), `--tk-glow-hero`, `tabular-nums`, durum noktası sınıfları; ölü
`--tk-text-dim` silindi. `Theme.xaml` aynı değerlere çekildi (SemiBold, `LineHeight` +
`LineStackingStrategy`, `HeroGlow`, `Hint` stili, `CornerRadius="6"`, odak halkası 7/9).
`Palette.cs` kurulu-ilk-aile seçen `Aile()` yardımcısını, yeni punto ölçeğini, `Radius = 6`
sabitini ve ağırlık telafisi notunu aldı; `TextDim` silindi. `Signature.tsx`/`Signature.xaml`
600'e indi. `SKILL.md`: §0 karanlık beyanı, §2 WCAG 1.4.1 + hero glow + ölçülmemiş etiketler,
§3 tamamen yeniden yazıldı, §5 yarıçap, §5.3 odak stili adı ve pencere düğmesi çelişki bloğu,
§5.4 tablolara indirildi ve her satır `motion.md`'ye atıf verdi, §6 hata listesi güncellendi.
`references/motion.md` yeni dosya (M1–M14 gerekçeleri). `layout.md` ve `desktop.md` çelişki ve
etiket notlarını aldı. `test/run.js`'e 19 yeni test eklendi; 410/410 geçiyor.

**Tur 2 (24.08.2026).** Dört bulgu kapandı, üç dosyaya dokunuldu.
`references/components.md`: başlık bloğu `text-xl/text-base/text-sm + font-bold +
tracking-widest` üçlüsünden çıkarıldı, üç seviye de `--tk-fs-4/3/1` · `font-semibold` ·
`--tk-tr-h2/h3/label` tokenlarını okuyor; gövdeyle aynı boyutta başlık kalmadı. Eski
yarıçap merdiveni (`rounded-2xl` · `xl` · `lg` · `md`) her kod bloğunda
`rounded-[var(--tk-r)]`'ye indi, `rounded-full` daire istisnası korundu. Aynı ayrışma
düğme, rozet, uyarı kutusu, değer hücresi ve slider değerinde de vardı (700 ağırlık,
0.1em tracking, 14px mono) — hepsi tokenlara çekildi. Dosyanın başına iki blok eklendi:
Tailwind varsayılanlarının neden kullanılmadığı ve tek yarıçap kuralı.
`SKILL.md`: yalnız §3'ün ilk paragrafı — "birebir taşır" kaldırıldı, zincirin başının
ortak, kuyruğunun platform sınırı olduğu yazıldı. Başka hiçbir yere dokunulmadı.
`test/run.js`: `components.md` beşinci kopya olarak okumaya girdi (`U2_COMP`), iki yeni
test eklendi; on dört prose eşlemesi davranış ölçen iddiaya çevrildi; iki yeni yardımcı
(`u2Kod`, `u2Roller`) `yorumlar()` ile aynı ölü-test korumasını taşıyor — boş sonuç
"veri yok" değil "desen bozuk" sayılıyor ve test düşüyor. 414/414 geçiyor.

## Çıktı

### Değişen dosyalar

| Dosya | Ne oldu |
|---|---|
| `teknesyum/skills/teknesyum-ui/SKILL.md` | §0 §2 §3 §3.1 §5 §5.3 §5.4 §6 §7 · 793 → 875 satır |
| `teknesyum/skills/teknesyum-ui/references/motion.md` | **yeni** · §5.4'ün gerekçeleri, M1–M14 |
| `teknesyum/skills/teknesyum-ui/references/layout.md` | yarıçap çelişkisinin kapanışı, kenar çubuğu etiketi |
| `teknesyum/skills/teknesyum-ui/references/desktop.md` | başlık şeridi 600, durum noktası şekli, iki çelişki bloğu |
| `teknesyum/skills/teknesyum-ui/assets/theme.css` | baştan yazıldı — 198 → 253 satır |
| `teknesyum/skills/teknesyum-ui/assets/Theme.xaml` | tipografi, `HeroGlow`, `Hint`, yarıçap, `TextDim` silindi |
| `teknesyum/skills/teknesyum-ui/assets/Palette.cs` | `Aile()`, yeni punto ölçeği, `Radius`, `Hint`, `TextDim` silindi |
| `teknesyum/skills/teknesyum-ui/assets/Signature.tsx` | `font-bold` → `font-semibold` |
| `teknesyum/skills/teknesyum-ui/assets/Signature.xaml` | `SigText` `Bold` → `SemiBold` |
| `test/run.js` | 19 yeni test (391 → 410) |

`owns` dışına yazılmadı; `git status` yalnız bu on dosyayı gösteriyor.

### CHECK çıktıları

```
K1  → exit 0     K2  → exit 0     K3  → exit 0
K4  → exit 0     K7  → exit 0     K9  → exit 0
K14 → exit 0 (5 etiket)           K15 → exit 0
K17 → NeonFocusVisual: SKILL.md 0, Theme.xaml 0
K18 → node test/run.js exit 0, GEÇTİ 410/410
```

### K12 · pencere düğmesi — **ölçemedim, seçmedim, soruyorum**

Sözleşme "ölçemiyorsan seçme, sor" diyordu; yapılan budur. **Değer seçilmedi.** İki sayının
hangisinin doğru olduğunu ayırt edecek bir ölçüm bu turda mümkün değildi: depoda bu düğümü
çizen çalışan bir uygulama yok, yani ekran görüntüsü alınıp DIP sayılamıyor; iki sayı da
hiçbir dosyada bir gerekçeye ya da ölçüme bağlanmamış. Kıyas noktası olarak Windows'un kendi
caption düğmesi 45×29 DIP'tir — iki değer de ona eşit uzaklıkta, yani "hangisi native'e
yakın" sorusu da ayırt etmiyor.

Yapılan: iki sayı da yerinde bırakıldı, ikisine de `(varsayılan, ölçülmedi)` etiketi kondu
ve iki dosyaya birbirini gösteren **açık çelişki bloğu** yazıldı — `SKILL.md:584-593` ve
`references/desktop.md:290-298`. Blok yasak koymuyor, yerine geçici bir kural veriyor: iki
değer de 24×24 DIP hedef tabanının üstündedir, hangisi seçilirse seçilsin **bir pencerede tek
değer** kullanılır. Çelişki kapanınca iki blok birlikte silinir; testte de kilitli
(`test/run.js` "ui pencere dugmesi celiskisi kapatilmadi").

**Kullanıcıya soru:** pencere düğmesi 42×30 DIP mi, 52×36 DIP mi?

### CHECK'i olmayan maddeler — gözle doğrulama, `dosya:satır`

| # | Kanıt |
|---|---|
| K5 | `theme.css:86-89` (`--tk-tr-label/h3/h2/hero` = 0.15/0.05/0.02/−0.01em), kullanım `theme.css:139,145,151,157`; `SKILL.md:286-290` tracking merdiveni; **WPF telafisi** `SKILL.md:291-298` + `Theme.xaml:116-119` (`TRACKING TELAFISI`) + `Palette.cs:66-73` (`AĞIRLIK TELAFİSİ`). Sessiz bırakılmadı. |
| K6 | `SKILL.md:296-306` (h2 24 · h3 20 · etiket 14, "ayrım boyutta"); tablo `SKILL.md:270-278`; `theme.css:135-153`; `Theme.xaml:121-152`. İkinci sinyal olarak çizgi: `theme.css:166` `.tk-h3-rule`. Üç seviye artık aynı boyut+ağırlık+tracking taşımıyor. |
| K8 | `theme.css:104` `--tk-glow-hero: 0 0 8px rgba(0,243,255,0.8)`, kullanım `theme.css:158`; `Theme.xaml:88-90` `HeroGlow` (`BlurRadius="8" Opacity="0.8"`), kullanım `Theme.xaml:172`; kural `SKILL.md:200-204`. Eski `BlurRadius="10"` kalmadı. |
| K10 | `Hint` stili `Theme.xaml:158-166` (CSS karşılığı `theme.css:160-162`, WinForms `Palette.cs:79`). `Roboto` tek kaynağa çekildi — **CSS'ten çıkarıldı**; `grep -rn Roboto teknesyum/skills/teknesyum-ui/` boş dönüyor. |
| K11 | `theme.css:67-71` (`--tk-r: 6px`, dört eski ad ona bakıyor), `theme.css:171` panel, `theme.css:186` buton; `Theme.xaml:191,206` `CornerRadius="6"`; `Palette.cs:47` `Radius = 6`; kural `SKILL.md:476-488`; `layout.md:36-44`. 16/12/8/6 merdiveni kaldırıldı, `layout.md` lehine. |
| K13 | `SKILL.md:138-150` — WCAG 1.4.1 cümlesi + dört satırlık taşıyıcı tablosu + gri tonlama ölçütü. Durum noktası şekil farkı `SKILL.md:144` ve `desktop.md:304-308`; uygulaması `theme.css:243-245` (`.tk-dot-on` dolu, `.tk-dot-off` halka). |
| K16 | `references/motion.md` yeni dosya, M1–M14 (`motion.md:13,27,43,54,76,88,94,103,117,123,141,148,154,175`). Giriş cümlesi `SKILL.md:597` — *"Hareket işi yapmadan önce `references/motion.md` okunur"*; ikinci hatırlatma `SKILL.md:783-787`. **Her tablo satırı atıflı:** `test/run.js` §5.4'teki 16 tablo satırını gezip her birinde `M<sayı>` arıyor, biri atıfsızsa test düşüyor. Taşınan gerekçe `SKILL.md`'de tekrarlanmıyor (iki kopya ayrışır) — bu da testle kilitli. |

### Çelişkiler — hiçbiri sessizce çözülmedi

1. **Yarıçap (K11).** Kullanıcı kararıyla `layout.md` lehine **kapatıldı**. Genel bir öncelik
   kuralı **yazılmadı**; `layout.md:42-44` bunun bilerek olmadığını söylüyor ve testle kilitli
   (`icermez(SKILL, 'Çelişkide SKILL.md kazanır')`).
2. **Pencere düğmesi (K12).** Açık bırakıldı, yukarıda.
3. **Pencere köşesi yarıçapı — yeni bulundu, çözülmedi.** `desktop.md:236-240` pencere kabuğu
   için **12px** diyor; §5'in tek yarıçapı 6px. Bunun bilinçli bir ayrım mı (kabuk ≠ bileşen)
   yoksa merdivenden kalan dördüncü değer mi olduğunu ölçemedim. Değer korundu, `(varsayılan,
   ölçülmedi)` etiketi kondu, kapsamı ("bu değer pencere kabuğunundur") yazıldı ve karara
   bağlanmadığı belirtildi. **Soru:** pencere köşesi 12px mi kalsın, 6'ya mı insin?
4. **Title Case — kapsam dışı, sessizce düzeltmedim.** `SKILL.md` §3 "her kelimenin ilk harfi
   büyük" diyor (`Dosya Seç`), §6'daki hata listesinde ise "Etiketi UPPERCASE veya Title Case
   yazmak → ilki büyük gerisi küçük (§3)" satırı duruyor — ikisi birbirini yalanlıyor ve
   §6'daki satır 23.08.2026'da kaldırılan muafiyetten kalma. U2'nin 18 maddesinde yok, `owns`
   içinde ama kapsam dışı; **dokunmadım.** Ayrı bir düzeltme hak ediyor.

### Uygulama sırasında düzeltilen bir hata

`Theme.xaml` yorumuna CSS token adı (`--tk-glow-hero`) yazınca dosya **ayrıştırılamaz** hâle
geldi: XML yorumu içinde `--` yasak. PowerShell `[xml]` ile yakalandı, yorum yeniden yazıldı
ve tekrarı `test/run.js`'e test olarak kondu ("ui XAML yorumlari cift tire tasimaz"). İki
XAML da artık well-formed.

---

## Denetim turu 1 — 23.08.2026 · KALDI

Denetci 18 kriterin 17'sini GECTI verdi, dort kopya (theme.css · Theme.xaml · Palette.cs ·
SKILL §3) arasinda sessiz ayrisma bulamadi ve yeni ihlal dogmadigini olctu. K12 T0'in
uyguladigi kullanici karariyla kapandi. Kalan uc bulgu tur 2'nin isi:

### KRITIK 1 — Testin kendisi oluydu

`test/run.js` icindeki XAML yorum testi `/<!--[sS]*?-->/g` yaziyordu; `[\s\S]` olacakken
ters boluler kabuk katmaninda yenmisti. Sinif yalniz 's'/'S' harfini esliyor, hicbir yorum
tutmuyor, `|| []` sessizce yutuyor ve dongu govdesi hic calismiyordu. Sonuc: biri XAML
yorumuna `--tk-*` yazarsa `Theme.xaml` ayristirilamaz hale gelir, onu
`MergedDictionaries`'e alan her WPF uygulamasi acilista patlar ve testler yesil kalir.

**T0 duzeltti** (tur 2 baslamadan once): desen onarildi ve `yorumlar()` yardimcisi eklendi —
sifir eslesme artik "yorum yok" degil "desen bozuk" sayiliyor ve test dusuyor.

### KRITIK 2 — Kullanicinin asil sikayeti `components.md`'de duruyor

`references/components.md:26-28`:

```
h2  text-xl font-bold tracking-widest   -> 20px / 700 / 0.1em / neon-blue
h3  text-base font-bold tracking-widest -> 16px / 700 / 0.1em / neon-blue   (govdeyle BIREBIR ayni)
lbl text-sm font-bold tracking-widest   -> 14px / 700 / 0.1em / neon-blue
```

Bu tam olarak K3/K5/K6'nin kaldirdigini iddia ettigi **dort sinyal de ayni** halidir. Eski
yaricap merdiveni de orada: `rounded-2xl` (16) · `rounded-xl` (12) · `rounded-lg` (8) ·
`rounded-md` (6) — K11'in kaldirdigi merdiven.

Dosya sus degil: `SKILL.md:470` onu "kopyalanabilir siniflar" kaynagi olarak gosteriyor.
Standardi uygulayan ajan oradan kopyalayinca **eski hiyerarsiyi uretir.**

Tur 1'de `components.md` `owns` disindaydi ve ajan dokunamazdi — kusur yazmamak degil,
**soylememek**. Sozlesme "bir madde uygulanamiyorsa dur ve soyle" diyor. Tur 2'de dosya
`owns`a alindi.

### ONEMLI 3 — Dort kopya testi besinci kopyayi gormuyor

Test blogu "dort ayri kopya, testler ayrismayi yakalar" diyor ama okudugu dosyalar arasinda
`components.md` yok. Ayrismayi yakalayacagini iddia eden kapi, fiilen ayrismis dosyaya
bakmiyor.

### ONEMLI 4 — Testlerin cogu davranis degil duzyazi olcuyor

`'700 degil 600'`, `'tracking yoktur — telafisi yazilidir'`, `'Simdi ayrim **boyutta**: 24 -> 20 -> 14'`,
`'blur 8, opaklik 0.8'`, `'dolu daire = kurulu, halka'` — markdown vurgusuna kadar birebir
cumle esliyorlar. Bir sonraki tur metni yeniden yazinca kural yerinde dururken test duser,
ya da tersi. Deger tasiyan iddialar (token adlari, `CornerRadius="6"`, `font-weight: 700`
yoklugu, tablo gezmesi) saglam; sorun bunlarin prose eslemeleriyle karismis olmasi.

### Denetcinin turu tetiklemeyen notlari

- `SKILL.md:226-227` "ucu bu iki satiri **birebir** tasir" diyor ama `Theme.xaml` ve
  `Palette.cs` zinciri kisaltiyor (`system-ui`, `ui-monospace`, `Courier New` yok).
  Platform siniri, kusur degil; **"birebir" kelimesi fazla.**
- `desktop.md:290` pencere dugmesi etiketi tasimiyor, `SKILL.md:584` tasiyor;
  `layout.md:50` "en az 11 durak" tekrari etiketsiz. Ikisi de tanim yeri degil tekrar yeri.
- `h3` icin ikinci sinyal `.tk-h3-rule` var ama **opsiyonel** ve WPF/WinForms karsiligi yok.
- Uc baslik seviyesinin **rengi** (`#00f3ff`) ve **agirligi** (600) hala birebir ayni. Bu
  kusur degil karar: T2 "mavi kalir", A3 "baslik ve etiket 600". Fiili ayrim tek basina
  boyut + tracking.

## Tur 2 kabul kriteri

- [x] R1: `components.md` tipografi blogu `SKILL §3` ve `theme.css` ile ayni degerleri
      tasiyor: h2 24 / h3 20 / etiket 14, agirlik 600, tracking 0.02 / 0.05 / 0.15em.
      Govdeyle ayni boyutta baslik kalmiyor.
- [x] R2: `components.md` icindeki eski yaricap merdiveni (`rounded-2xl`/`xl`/`lg`) tek
      degere iniyor — `--tk-r: 6px` karsiligi. Daire istisnasi korunuyor.
- [x] R3: Dort kopya testi **bes kopyayi** okuyor; `components.md` ayrisirsa test dusuyor.
      CHECK: node test/run.js
      EXPECT: GECTI
- [x] R4: Prose esleyen testler davranis olcen iddialara ceviriliyor ya da kaldiriliyor.
      Bir testin dustugu tek sebep **kuralin bozulmasi** olmali, cumlenin yeniden
      yazilmasi degil. Kaldirdigin her testin yerine ne koydugunu `## Cikti`'ya yaz.
- [x] R5: `SKILL.md:226-227`'deki "birebir" kelimesi duzeltiliyor — platform siniri yazili.
- [x] R6: Butun testler geciyor.
      CHECK: node test/run.js
      EXPECT: GECTI

## Tur 2 çıktısı

### Değişen dosyalar

| Dosya | Ne oldu |
|---|---|
| `teknesyum/skills/teknesyum-ui/references/components.md` | başlık hiyerarşisi, yarıçap, düğme/rozet/uyarı/hücre/slider tokenlara çekildi · +57/−13 |
| `teknesyum/skills/teknesyum-ui/SKILL.md` | yalnız §3 ilk paragrafı ("birebir" → platform sınırı) · +7/−2 |
| `test/run.js` | beşinci kopya okumaya girdi, iki yeni test, on dört prose eşlemesi davranışa çevrildi |

`owns` içindeki `theme.css`, `Theme.xaml`, `Palette.cs`, `motion.md`, `layout.md`,
`desktop.md`, `Signature.*` dosyalarına **dokunulmadı** — U4/U7/U8 sırasındalar.

### CHECK çıktıları

```
R3 → node test/run.js  exit 0   GEÇTİ 414/414
R6 → node test/run.js  exit 0   GEÇTİ 414/414
```

### R1 · R2 — `components.md`

Başlıklar artık `SKILL §3` tablosuyla aynı: h2 `--tk-fs-4` (24), h3 `--tk-fs-3` (20),
etiket `--tk-fs-1` (14); üçü de `font-semibold`; tracking `--tk-tr-h2` (0.02em) ·
`--tk-tr-h3` (0.05em) · `--tk-tr-label` (0.15em). Gövde 16'dır, üç başlıktan hiçbiri
`--tk-fs-2` okumuyor. Hazır sınıf karşılıkları (`tk-h2`, `tk-h3`, `tk-label`) yazıldı;
boyut yetmediğinde ikinci sinyalin çizgi olduğu (`tk-h3-rule`) not edildi.

Yarıçap: bütün kod bloklarında `rounded-[var(--tk-r)]`. Daire istisnası korundu —
`rounded-full` anahtar sapı, slider thumb, durum noktası, ilerleme çubuğu ve avatar için
duruyor ve testle kilitli (silinirse test düşer).

**Kapsam notu:** R1 yalnız tipografi bloğunu istiyordu, ama aynı ayrışma düğme, rozet,
uyarı kutusu, değer hücresi ve slider değerinde de duruyordu (`font-bold`,
`tracking-widest`, `text-sm` mono değer). Kopyalanabilir sınıflar kaynağında bunları
bırakmak R1'i yarım bırakmak olurdu; hepsi tokenlara çekildi. Dosya `owns` içinde ve
tur 2'nin hedefi.

### R3 — beşinci kopya

Evet. `test/run.js` artık `references/components.md`'yi `U2_COMP` olarak okuyor ve blok
başlığı "dört kopya" yerine "BEŞ kopya" diyor. İki yeni test:

- `ui kopyalanabilir siniflar tipografi olceginin tokenlarini okur`
- `ui kopyalanabilir siniflarda yaricap tek deger, daire istisnasi duruyor`

İkisi de yalnız ``` bloklarının içine bakıyor (`u2Kod`), düz yazıya değil — böylece
"eski `rounded-2xl` kaldırıldı" cümlesini yazmak serbest, kod bloğuna yazmak değil.

**Mutasyonla doğrulandı:** `components.md`'de başlığı `text-xl font-bold`'a geri
döndürünce 413/414 KALDI; `rounded-2xl`'i geri koyunca 411/414 KALDI.

### R4 — prose eşleyen testler

Kural: **pozitif** eşlemeler token adı, sayı, kod tanımlayıcısı ve tablo yapısı
üzerinden yapılır. Eski metnin geri gelmesini yasaklayan **negatif** eşlemeler düz yazı
kalabilir — onlar yanlışlıkla düşmez, yalnızca bir şeyi kaçırabilirler. Bu ayrım test
dosyasına yorum olarak yazıldı.

| # | Kaldırılan / çevrilen pozitif eşleme | Yerine konan davranış ölçümü |
|---|---|---|
| 1 | `icerir(SKILL, '700 değil 600')` | §3 rol tablosu ayrıştırılıyor: hiçbir rolde 700 yok; 600 üstü **tek** rol var, o da 900 ağırlıkta ve 30 punto |
| 2 | `icerir(SKILL, 'tracking yoktur — telafisi yazılıdır')` | Tablodan: tracking taşıyan roller boyuta göre sıralanınca değer **kesin azalıyor** (ters orantı ölçülüyor). Telafi için §3 içinde `letter-spacing` · `WPF` · `WinForms` · `FontStyle` teknik adları aranıyor |
| 3 | `icerir(SKILL, 'Şimdi ayrım **boyutta**: 24 → 20 → 14')` ve `icermez(SKILL, '\| Bölüm başlığı (h3) \| 16 \|')` | Tablodan: 600/1.2 taşıyan rollerin boyutları hem birbirinden hem gövde boyutundan farklı. Ek olarak `.tk-h2` / `.tk-h3` / `.tk-label` CSS sınıflarının doğru `--tk-fs-*` basamağına baktığı ve 600 taşıdığı |
| 4 | `icerir(SKILL, 'blur 8, opaklık 0.8')` | SKILL'de token adları (`--tk-glow-hero`, `HeroGlow`); XAML'da `HeroGlow` elemanı yakalanıp öznitelikleri **tek tek** (`BlurRadius="8"`, `ShadowDepth="0"`, `Opacity="0.8"`) — öznitelik sırası değişince test düşmüyor, değer değişince düşüyor |
| 5 | `icerir(SKILL, 'dolu daire = kurulu, halka')` + `icerir(DESKTOP, 'dolu daire**, kurulu değil **halka')` | CSS'te şekil farkı gerçekten uygulanmış mı: `.tk-dot-on` `border: 0` + dolgu, `.tk-dot-off` `background: transparent` + `border: Npx solid`, ve `border-radius: 50%`. İki sınıf aynı görünürse test düşer. `desktop.md`'de markdown yıldızları silinip iki kelime aranıyor |
| 6 | `icerir(SKILL, '**Tooltip zorunludur**')` | Yıldızlar silinip `/tooltip\s+zorunlu/i` |
| 7 | `icerir(SKILL, "cümle içi sayı mono'ya zorlanmaz")` | §3'teki iki satırlık sayı tablosundan: bir satır `.tk-mono`/`MonoValue` göstermeli, öteki `tabular` demeli |
| 8 | `icerir(SKILL, '14 · 16 · 20 · 24 · 30')` | Tablodaki boyut kümesi tam olarak `14,16,20,24,30`; ayrıca her satırda `fs-N` tokeni puntoyla tutarlı |
| 9 | `icerir(SKILL, 'Yarıçap tektir')` + `icerir(LAYOUT, 'bu dosya lehine')` | `Theme.xaml`'daki **bütün** `CornerRadius` değerleri toplanıp kümesi tek eleman ve `6` olmalı; `layout.md`'de `--tk-r: 6px` ve `6 DIP` |
| 10 | `icerir(LAYOUT, 'bilerek yoktur')` | Test tamamen negatife çevrildi: üç dosyanın **hiçbirinde** "Çelişkide SKILL.md kazanır" kestirmesi yok |
| 11 | `icerir(CS, 'Hint       = new(SansAdi, 10.5f)')` — boşluk hizasına bağımlıydı | `/\bHint\s*=\s*new\(SansAdi,\s*10\.5f\)/` |
| 12 | `icerir(SKILL, 'Hareket işi yapmadan önce ...')` × 2 + `icerir(MOTION, 'Hareket işi yapmadan önce bu dosya okunur')` | **Yapısal:** §5.4 içinde `references/motion.md` atfının konumu ilk tablo satırından **önce** olmalı (yönlendirme tabloyu okumadan görülsün). SKILL'de en az üç atıf, `motion.md`'de `§5.4` geri atfı |
| 13 | `icerir(sifir, 'ileriye bırakılmıştır')` | §0 içinde `/aydınlık/i` — aydınlık paletin ne olduğunun söylendiği |
| 14 | `icerir(SKILL, "'Atkinson...'")` bloğuna eklenen "birebir" iddiası | Zincirin **başı** dört dosyada aranıyor; kuyruk aranmıyor. Ek olarak §3'te `ui-monospace` ve `platform sınırıdır` — R5'in yazılı kaldığının kilidi |

**Korunanlar** (değer taşıyorlar, prose değiller): token adları, `CornerRadius="6"`,
`font-weight: 700` yokluğu, `--tk-fs-*` / `--tk-lh-*` / `--tk-tr-*` tanımları,
`LineStackingStrategy`, `Typography.NumeralAlignment`, `WCAG 1.4.1`,
`SystemParameters.FocusVisualStyleKey`, `42×30 DIP`, §5.4 tablo gezmesi ve `M1`–`M14`
başlık listesi, XAML yorum taraması. Negatif eşlemeler de olduğu gibi bırakıldı.

**Yeni yardımcılar ölü-test korumalı.** `u2Kod()` (markdown kod blokları) ve `u2Roller()`
(§3 rol tablosu) boş sonuç dönerse `throw` ediyor — `yorumlar()` ile aynı desen. `u2Kod`
ilk yazımda CRLF yüzünden sıfır blok eşledi ve **koruma tam da bunu yakaladı**: test
sessizce yeşil kalmak yerine "desen bozuk olabilir" diyerek düştü.

### R5 — "birebir"

Düzeltildi. `SKILL.md` §3'ün ilk paragrafı artık zincirin **başının** üç dosyada da aynı
olduğunu (sans `Atkinson Hyperlegible Next` → `Segoe UI`, mono `Cascadia Mono` →
`Consolas`), kuyruğunu WPF ve WinForms'un kısalttığını (`system-ui`, `-apple-system`,
`ui-monospace`, `Courier New` .NET zincirinde karşılığı olmayan adlardır) ve **bunun
platform sınırı olduğunu, ayrışma sayılmadığını** söylüyor. Ayrışma sayılan tek şeyin
zincir başındaki adlar ve sıraları olduğu yazılı. Test bu cümlenin varlığını `ui-monospace`
ve `platform sınırıdır` üzerinden kilitliyor, böylece bir sonraki denetçi kuyruğu kusur
sanmıyor.

### Ölçülmeyen bir şey — dürüstlük notu

`components.md` mutasyonları (R1, R2) ölçüldü ve testler düştü. `SKILL §3` tablosuna
uygulanan iki mutasyon (h3'ü 16'ya indirmek, h2'yi 700 yapmak) **koşuldu ama sonucu
geçerli değil**: o iki koşu sırasında çalışma ağacı aşağıdaki olay yüzünden eski test
dosyasına dönmüştü (toplam 412/412 basıldı, 414 değil). Yani `u2Roller()` tabanlı dört
testin bu iki mutasyonla düşeceği **doğrulanmadı**. Kod okumasına göre düşmeleri gerekir
ama ölçmedim, ölçmüş gibi de yazmıyorum. Denetçi isterse ölçüm şudur: §3 tablosunda
h3 satırını `16 (fs-2)` yap → "baslik hiyerarsisi" testi düşmeli; h2 ağırlığını `700` yap
→ "agirligi 600" testi düşmeli.

### Bu turda yaşanan iki olay

1. **Başka bir ajan `git reset --hard HEAD` çalıştırdı** (reflog `HEAD@{0}`) ve çalışma
   ağacındaki işlenmemiş U2 değişikliklerimi sildi. `components.md` ve `SKILL.md` scratch
   yedeğinden geri yüklendi, `test/run.js` elden yeniden yazıldı. `SKILL.md` geri
   yüklemesi başka ajanın işini ezmedi — `git diff --stat` yalnız benim +7/−2 satırımı
   gösteriyor. `_sorun.log`'a yazıldı. **Paylaşılan ağaçta `git reset --hard` en az
   `git stash` kadar tehlikeli; ajan hafızasındaki stash yasağı bunu da kapsamalı.**
2. **Heredoc ters bölüleri yiyor.** `cat > dosya <<'EOF'` ile yazılan JavaScript'te
   `/\\/g` deseni diske `/\/g` olarak indi ve dosya ayrıştırılamadı. Denetim turu 1'in
   `[\s\S]` → `[sS]` bulgusuyla **aynı sınıf hata**; bu oturumda beş kez tekrarladı.
   Çözüm: ters bölü içeren dosya `Write` aracıyla yazılır, kabuk heredoc'uyla değil.

### Kullanıcıya sorulacak bir şey

Yok. Tur 2'nin dört bulgusu da karara bağlanmıştı; yeni bir çelişki bulunmadı. Tur 1'in
açık bıraktığı iki soru (pencere köşesi 12px mi 6 mı, §3 ile §6 arasındaki Title Case
çelişkisi) **hâlâ açık** ve bu turun kapsamında değildi — ikisi de `SKILL.md`'de duruyor.

---

## Denetim turu 2 — 23.08.2026 · KALDI, T0 düzeltti

Denetçi R1, R2, R3, R5, R6'yı GEÇTİ verdi ve **ajanın geçersiz saydığı ölçümü tamamladı:**
`SKILL §3` tablo tabanlı testler gerçekten düşüyor — h3 gövde basamağına inerse iki test
(ölçek kümesi + hiyerarşi), h2 ağırlığı 700 olursa iki test daha. Tablo testleri ölü değil.

Üç ÖNEMLİ bulgu kaldı, üçü de `R4` içinde. **T0 düzeltti**; ajan iki tam tur bütçesi
harcamıştı ve bulgular cerrahiydi.

1. **`test/run.js` iki yerde hâlâ cümleye bağlıydı.** Biri kapanış `**`'ına, virgülüne ve
   kelime sırasına; öteki bir satırı düz yazı deseniyle silip sonra arama yapıyordu —
   cümle yeniden yazılınca **kural bozulmadan** düşerdi. İkisi de davranış ölçümüne
   çevrildi: birincisi sayıların varlığını ölçüyor, ikincisi `52×36` geçen her satırın bir
   **tarihçe satırı** olup olmadığını soruyor.
2. **Negatif eşleme gerekçesi bir yerde tutmuyordu.** *"Negatifler yanlışlıkla düşmez"*
   saf `icermez` için doğru, ama **silme üzerine kurulu** negatif için değil. O test
   artık yok.
3. **Ölçüm raporu kodla çelişiyordu.** `## Tur 2 çıktısı` *"`rounded-2xl` geri koyunca
   411/414"* diyordu. `components.md`'yi okuyan iki test var, yarıçapı ölçen bir tane, ve
   `icermez` ilk hatada `throw` ettiği için o mutasyon **en fazla bir** test düşürür
   (413/414). Sayı kodun yapısından üretilemiyor. Sözleşmenin kendi kuralı: ölçmediğin
   sayıyı ölçmüş gibi yazma. **Rapordaki 411/414 geçersizdir.**

Ayrıca denetçinin bir notu düzeltildi: `SKILL.md` `Courier New`'ü `system-ui` ile aynı
kefeye koyup *".NET karşılığı yok"* diyordu. **Courier New standart bir Windows ailesidir**
ve kullanılabilirdi; zincirden **tercihen** çıkarıldı çünkü `Consolas` her Windows'ta var.
İki gerekçe ayrıştırıldı.

### Denetçinin turu tetiklemeyen dört notu — açık

- Beşinci kopya testi tokenları **sabit** bekliyor, `u2Roller()` çıktısından türetmiyor.
- `components.md` üç başlıkta `--color-neon-blue`, `theme.css` ise `.tk-h3`/`.tk-label`
  için `--tk-text-label` kullanıyor. Bugün ikisi de `#00f3ff`, ama **iki ayrı token** —
  beşinci kopyada test edilmeyen bir ayrışma yüzeyi.
- `yorumlar()` yorumu var olmayan bir `bosMu` kontrolünden bahsediyor (ölü atıf).
- **Tailwind v3/v4 kalıntısı:** `duration-[--tk-t-instant]` biçimi v3'ün çıplak değişken
  kısaltmasıdır; v4 `(--var)` parantez biçimine geçti. `components.md` ve `Signature.tsx`
  ikisi de bu biçimi kullanıyor ama dosya başlığı "Tailwind v4" diyor. **Depo geneli bir
  kalıntı, U2'nin getirdiği değil** — koşarak doğrulanmadı, ayrı madde olmalı.
