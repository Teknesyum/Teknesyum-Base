---
id: Y7
title: premiumNotu talep üzerine yüklensin — enjeksiyonun %83'ü
role: builder
depends: []
owns: [teknesyum/hooks/dil.js, teknesyum/skills/relay/references/premium.md, test/run.js]
side_effects: []
risk: high
status: submitted
round: 1
---

# Y7 — Büyük güç gerektiğinde gelsin

## Ölçü

`scripts/olcum/yuz-milyon.js` (27.08.2026): eklentinin 100M token içindeki toplam
yükü **$6,43**. En büyük tek kalem **enjeksiyon: $2,36 (%36,8)**. Enjeksiyonun
içinde:

| anahtar | token (tek dil) | ne yapıyor |
|---|---:|---|
| `premiumNotu` | **~836** | premium doktrini — paralellik, model seçimi, konsey, ikinci görüş |
| `ecoNotu` | ~105 | eco doktrini |
| `dilTalimati` | ~36 | ajanlara hangi dilde yazılacağı |

`premiumNotu` premium oturumda bağlama bir kez yazılır ve **o oturumun her turunda
yeniden okunur**. 60 turluk bir oturumda 836 × 60 ≈ 50.000 token.

## Kullanıcının koyduğu mimari

> "Bir projede bir sohbet açtık, maliyeti ödedik; bir daha maliyet ödemeden premium
> işleyişi yakalayalım. Çok tekrarlanan bir şey varsa MCP'lerle aynı mantıkla
> yaklaşacağız: yük çok az olacak, program gerektiğinde o büyük güce kavuşabilecek."

Yani: **bağlamda emir kalır, gerekçe dosyaya iner.** Model doktrini uygulamak için
her turda 836 token taşımaz; ne yapacağını bilecek kadarını taşır, nedenini merak
ederse okur.

## İş

### 1. `premiumNotu`yu ikiye ayır

**Bağlamda kalacak (hedef ≤150 token, tek dil):** yalnız davranışı değiştiren emirler.
Ölçüt şu: cümle çıkarılınca model farklı davranır mı? Hayırsa gövdeye iner.

Korunması zorunlu emirler (bunlar davranış değiştirir, kısaltılabilir ama **düşemez**):
- her ajan opus; sonnet/haiku yok
- bağımsız sözleşmeler sıraya alınmaz, aynı anda koşar — 20'ye kadar paralel,
  üçten sonra worktree yalıtımı
- paralel varsayılan; tek ajan gerekçe ister
- ajan açmak için kullanıcı izni beklenmez
- token tasarrufu burada gerekçe değil
- deterministik alet model çağrısından önce gelir
- plan konseyi açık: sıfırdan projede PLAN.md'den önce iki planner (fable + opus)
- ikinci görüş açık

**Gövdeye inecek:** gerekçeler, örnekler, "neden böyle" açıklamaları, düşünme
seviyesi ayarı anlatımı, konsey çıktısının PLAN.md'de nasıl yazılacağının detayı.

Gövde dosyası: `teknesyum/skills/relay/references/premium.md`. Bağlamdaki kısa metnin
son satırı bu dosyaya işaret etsin — model gerektiğinde okusun.

### 2. `ecoNotu` aynı muameleyi görmez

211 token (iki dil), zaten kısa ve eco profilinin bütün anlamı "az yük". Dokunma.

### 3. Ölçüyü kaydet

Değişiklikten sonra `node scripts/olcum/yuz-milyon.js --json` koş ve
`enjeksiyon.yuzMilyonUsd` değerinin önce/sonra karşılaştırmasını sözleşmenin
Kayıt noktası bölümüne yaz.

## Kabul kriterleri

1. `premiumNotu` bağlam metni **≤150 token** (tek dil, `scripts/olcum` katsayısı 2,492
   ile hesapla) ve yukarıdaki sekiz emrin **hepsi** metinde hâlâ var — hangisinin
   hangi cümleyle karşılandığını tabloya yaz
2. `teknesyum/skills/relay/references/premium.md` var, gövdeye inen her şey orada,
   hiçbir cümle kaybolmamış — silinen değil taşınan
3. Bağlamdaki kısa metin bu dosyanın yolunu **ismen** söylüyor
4. `node test/run.js` ve `node test/all.js` yeşil — ham çıktı rapora. Premium
   enjeksiyonunu sınayan mevcut testler yeni metne göre güncellendi ve **neden**
   güncellendiği yazıldı
5. `scripts/olcum/yuz-milyon.js --json` çıktısında enjeksiyon kalemi düştü, önce/sonra
   sayısı Kayıt noktasında
6. `ecoNotu`, `dilTalimati` ve `dil.js` içindeki başka hiçbir anahtar değişmedi

## Risk

Bu metin modelin premium davranışını üreten tek yer. Kısaltmak paralelliği
söndürebilir — eklentinin asıl iddiası budur. Emir listesinden bir madde sessizce
düşerse kimse fark etmez, sadece eklenti eskisi gibi çalışmaz. Kabul 1'deki tablo
bunun için var: her emrin karşılığını göster.

---

## Kayıt noktası

Yapılanlar:

1. `dil.js` · `premiumNotu` yeniden yazıldı: 2088 karakter / 838 token → **372 karakter /
   149,3 token** (katsayı 2,492). `tr` ve `en` metinleri aynı; ayrıştıkları tek yer olan
   `Teknesyum ▸ Görüş ▸ / Opinion ▸` satırı gövdeye indi. `GORUS` sabiti hâlâ kullanılıyor
   (`advisor (fable)`).
2. `teknesyum/skills/relay/references/premium.md` yazıldı (9 başlık). Bağlamdan inen her
   cümle orada: her başlıkta önce Türkçe gerekçe, sonra özgün İngilizce cümle blok alıntı
   olarak birebir duruyor. Otomatik denetim: eski metnin 25 karakterden uzun 24 cümlesi
   tarandı, 24'ü de gövdede bulundu (iki cümle biçim farkıyla eşleşti: `(Max 20x)` cümlesi
   giriş paragrafında alıntılı, advisor cümlesi `GORUS` yerine `fable` yazılmış haliyle).
3. `test/run.js` iki test güncellendi (gerekçeleri dosyada yorum olarak yazılı).

Kabul 1 — sekiz emrin karşılığı:

| # | Emir | Bağlamdaki cümle |
|---|---|---|
| 1 | her ajan opus; sonnet/haiku yok | `Agents: opus only, no sonnet/haiku.` |
| 2 | bağımsız sözleşmeler sıraya alınmaz, 20 paralel, üçten sonra worktree | `Independent contracts run at once: 20 parallel, worktree past 3.` |
| 3 | paralel varsayılan; tek ajan gerekçe ister | `Parallel is default; one agent needs a reason.` |
| 4 | ajan açmak için izin beklenmez | `Open agents without asking.` |
| 5 | token tasarrufu burada gerekçe değil | `Tokens are not a reason.` |
| 6 | deterministik alet model çağrısından önce | `Deterministic tool before model.` |
| 7 | plan konseyi: sıfırdan projede PLAN.md'den önce iki planner (fable + opus) | `New project: fable+opus plan council before PLAN.md.` |
| 8 | ikinci görüş açık | `Second opinion: advisor (fable).` |

Metnin tamamı (372 karakter):

> Premium mode is on. Agents: opus only, no sonnet/haiku. Independent contracts run at
> once: 20 parallel, worktree past 3. Parallel is default; one agent needs a reason. Open
> agents without asking. Tokens are not a reason. Deterministic tool before model. New
> project: fable+opus plan council before PLAN.md. Second opinion: advisor (fable).
> Why: relay/references/premium.md

Emir 1'de "no sonnet/haiku" ayrı yazıldı, çünkü "opus only" tek başına yasağı ima ediyor
ama sözleşme iki yarımı da istiyor. Emir 7'de "sıfırdan proje" koşulu `New project:` ile
korundu — düşseydi konsey her PLAN.md'de açılırdı.

Kabul 4 — ham test çıktısı:

```
node test/run.js   → ✓ GEÇTİ  573/573
  ✓ premium notu sekiz emri tasir, gerekcesi govdeye iner   (satır 223)
  ✓ eco notu premium notundan uzun degil                    (satır 238)
node test/all.js   → ✓ 10 suite geçti
```

Güncellenen iki test ve nedeni:

- `premium notu paralel acmayi varsayilan sayar` → `premium notu sekiz emri tasir,
  gerekcesi govdeye iner`. Eski iddialar `the user asks for a plan` ve `Teknesyum ▸ Görüş ▸`
  bağlam metninde aranıyordu; ikisi de gerekçe cümlesi, gövdeye indi. Bağlamda aranmaları
  metnin uzun kalmasını zorunlu kılıyordu. Yeni test iki parçalı: sekiz emrin sekizi de iki
  dilde bağlamda mı (+ gövde yolu adı geçiyor mu + not 150 tokenin altında mı), taşınan dört
  cümle gövdede mi.
- `eco notu premium notunun yarisini gecmez` → `eco notu premium notundan uzun degil`.
  Oran iddiası premium notunun uzun kalmasına dayanıyordu (2088'e karşı 263 karakter).
  Premium 372'ye inince aynı oran eco'yu 186 karaktere kısmaya zorlar; oysa sözleşme
  `ecoNotu`'na dokunulmamasını söylüyor. İddia yön değiştirdi: eco ≤ premium ve eco kendi
  150 token tavanının altında.

Kabul 5 — enjeksiyon ölçüsü:

`scripts/olcum/yuz-milyon.js` enjeksiyon değerini ölçmüyor, `OLCUM.enjeksiyon = 1005`
sabitinden okuyor. Bu dosya `owns` listemde değil, sabiti güncellemedim. Yeni değer
1005 − (838 − 149) = **316 token**. Sabit elle verilerek koşulan ölçüm:

| | token | yuzMilyonToken | yuzMilyonUsd |
|---|---:|---:|---:|
| önce (1005) | 1005 | 1.322.003 | **$2,3631** |
| sonra (316) | 316 | 415.675 | **$0,7430** |

Düşüş $1,62 — enjeksiyon kalemi %68,6 azaldı.

Kabul 6: `ecoNotu` (263 karakter) ve `dilTalimati` (91 karakter) değişmedi, `dil.js`
farkında bu iki anahtar yok.

## Rapor

- `scripts/olcum/yuz-milyon.js` sabiti `owns` dışında olduğu için güncellenmedi — T0 kararı
  gerekli: `OLCUM.enjeksiyon` 1005 → 316.
- `Prior art in this mode means 50 repositories.` cümlesi sekiz emrin arasında değildi,
  150 token bütçesine sığmadı, gövdeye §9 olarak indi. Davranış değiştiren bir sayı olduğu
  için not düşüyorum.
- Kapsam dışı gözlem: `dil.js` içindeki `premiumAcik` anahtarı ben dosyayı okumadan önce
  silinmişti (git farkında görünüyor, benim değişikliğim değil). Başka bir ajanın işi
  olabilir; dokunmadım.

## Çıktı

`premiumNotu` 838 → 149 token; gerekçe `references/premium.md` gövdesine taşındı, hiçbir
cümle silinmedi. Testler 573/573 + 10 suite yeşil. Enjeksiyon kalemi $2,3631 → $0,7430
(sabit güncellemesi T0'da).
