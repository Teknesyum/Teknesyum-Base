---
id: U8
title: Glow performans kuralı ve ölçümü
role: ui-builder
depends: [U2]
owns: [teknesyum/skills/teknesyum-ui/references/motion.md, test/u8-glow.js]
side_effects: []
risk: critical
status: submitted
round: 1
agent_id: ui-builder
audit: —
auditor_id: —
diff: teknesyum/skills/teknesyum-ui/references/motion.md, test/u8-glow.js
verification: node test/u8-glow.js → GEÇTİ (77 doğrulama)
---
## Amaç

Glow standardın en pahalı görsel efekti ve tek satır performans uyarısı taşımıyor.
WPF `DropShadowEffect` alt ağacı ara yüzeye render eder ve kaydırmada her karede yeniden
çizer; CSS tarafında `backdrop-filter: blur(16px)` ile 40px gölge yığılması da ölçülmemiş.

T0 "10'dan fazla glow'lu öğede liste glow'u kalkar" önerdi; **fable reddetti** — sayı
uydurma ve yanlış proxy. Yerine geçen kural aşağıda.

**Bu sözleşme kendi konseyini açar.** Kullanıcı 23.08.2026'da şöyle dedi: *"7 maddeyi
kendi sözleşmesi ve kendi konseyiyle detaylıca inceleyip yap, hepsini abine danış."*
Yani: iş başlamadan önce `fable` ile ayrı bir görüş alınır, çıkan öneri sözleşmenin
`## Konsey` bölümüne yazılır, ancak sonra kod yazılır.

## Kapsam ve dalga

**`owns` bilerek dar.** Yedi sozlesmenin yedisi de basta `SKILL.md` ve `test/run.js`
istiyordu; protokol `owns` cakismasini yasakliyor ve kanca diff kesisimini denetliyor,
yani o hâlleriyle paralel kosamazlardi. Cozum: **her sozlesme kendi yeni dosyalarina
yazar.** Paylasilan dort dosya — `SKILL.md`, `references/components.md`,
`references/desktop.md`, `test/run.js` — hicbirinin `owns`unda degil ve tek bir
birlestirme sozlesmesine (`U10`) birakildi.

Bu senin isini degistirmiyor, yerini degistiriyor:

- Kurali ve kalibi **kendi referans dosyana** tam yaz. Eksik birakip "SKILL'e yazilir"
  deme; U10 yalnizca isaret koyar, icerik uretmez.
- Testini **kendi test dosyana** yaz. `test/run.js`'e dokunma; U10 onu dahil eder.
  Dosyan tek basina kosabilir olsun: `node test/<dosyan>` gecmeli.
- `SKILL.md`'ye girmesi gereken bir cumle varsa `## Cikti` bolumune **birebir yazilacak
  metin** olarak koy. U10 onu oradan alir.

Dalga: **A**. Ayni dalgadaki sozlesmeler ayni anda kosar; sonraki dalga oncekinin
cekirdek asset degisikliklerinin uzerine biner.

## Kabul kriteri

- [x] K1: **Yapısal kural:** glow kaydırılabilir/sanallaştırılmış tekrar eden öğeye
      (liste satırı, grid hücresi, tablo satırı) verilmez, kapsayıcı panele verilir.
      Üç satır da olsa üç yüz satır da olsa aynı — sayı eşiği yok.
- [x] K2: **Ölçüm kriteri:** kaydırma sırasında kare süresi ≤ 16 ms (60 fps). Web'de
      DevTools Performance, WPF'te `CompositionTarget.Rendering` ya da PerfView.
- [x] K3: **Platform ayrımı:** WPF `DropShadowEffect` kaydırılan yüzeyde mutlak yasak;
      CSS statik `box-shadow` ucuz, asıl pahalı olan `backdrop-filter` yığılması ve gölge
      animasyonu (§5.4 zaten yasaklıyor, atıf yeter). WPF satırı sert, CSS satırı yumuşak.
- [x] K4: 24px pay kuralının bilinen iki muafiyeti yazıldı: **inset glow** ve **scrollbar**.
      Bugün ikisi de kuralı çiğniyor; kural pratiği yakalasın, pratik kuralı değil.
- [x] K5: En az bir gerçek ölçüm yapıldı ve sayısı yazıldı. Ölçülemiyorsa neden
      ölçülemediği yazıldı — "ölçülmedi" demek serbest, ölçmüş gibi yazmak değil.
- [x] SON: Kendi test dosyan tek başına geçiyor ve her yeni davranışın testi var.
      CHECK: node test/u8-glow.js
      EXPECT: GEÇTİ


## Konsey

**fable · 23.08.2026.** Görüş bağlayıcı değil ama katılmıyorsan gerekçeni yaz.

### 1 · Kural ikiye ayrılır — bölüşme doğru

`motion.md`'ye yalnız **hareketle kesişen** kısım girer: yeni bir `M15 · Glow ve kaydırma`
başlığı altında yapısal kural + WPF `DropShadowEffect` yasağı + 16 ms ölçütü. Gerekçe:
kuralın tetikleyicisi kaydırmadır, kaydırma bir hareket olayıdır. `M6` zaten `box-shadow`
animasyon yasağını taşıyor — **M15 ona atıf verir, tekrarlamaz.**

Glow'un **yüzey** tarafı (token, 24px pay, muafiyetler) `SKILL §2`'nin malı. `U8` onu
`## Çıktı

### 1 · Yapılan iş

- `teknesyum/skills/teknesyum-ui/references/motion.md` — yeni **M15 · Glow ve kaydırma**
  başlığı: yapısal kural + kapsam tablosu + "yerine ne konur" + WPF `DropShadowEffect`
  yasağı + CSS/WPF ayrımı + 16 ms ölçüm reçetesi. Gölge animasyonu yasağı tekrarlanmadı,
  **M6**'ya atıf verildi. Giriş satırındaki atıf aralığı `M1 … M14` → `M1 … M15` oldu.
- `test/u8-glow.js` — yeni. Tek başına koşuyor, `GEÇTİ` basıyor.

Konseyin dört bağlayıcı maddesine de uyuldu; katılmadığım bir madde yok.

### 2 · `SKILL §2`'ye birebir girecek metin (U10 taşır)

Aşağıdaki blok `SKILL.md` §2'de glow paragrafının ("Glow **kutuya** uygulanır…") hemen
altına, opaklık merdiveni paragrafından önce girer.

<<<SKILL-2-BASLA
**Glow'un payı: 24px.** Dışa taşan bir hale komşusuna 24px'ten yakınsa komşunun altında
kesilir ve kirli bir kenar bırakır. Kural: dışa taşan glow taşıyan her yüzeyin çevresinde
en az **24px** boşluk bulunur (§8 aralık merdiveninin en üst basamağı). Boşluk yoksa glow
konmaz — yerine `/50` kenarlık kullanılır.

**Kuralın sınırı ve tek muafiyeti.** Pay kuralının gerekçesi dışa taşan halenin komşuda
kesilmesidir; kapsam o gerekçeyle çizilir:

- **Inset glow** (`inset 0 0 8px <renk>`) dışa taşmaz → pay talebi hiç doğmaz. Muafiyet
  değil, **kuralın tanım alanı dışı.**
- **Scrollbar** tek gerçek muafiyettir: 10px yolda 24px pay fiziksel olarak imkânsız ve
  thumb'ın komşusu yok, kesilme görünmez.
- **`--tk-glow-hero`** (blur 8, `drop-shadow`) taşması küçüktür ama vardır: hero sayı
  panel kenarına 24px'ten yakınsa aynı pay kuralına girer.

Tek cümlede: *24px payı yalnız dışa taşan glow ister; taşmayan (inset) ve kesişecek
komşusu olmayan (scrollbar) yüzeyler kapsam dışıdır.*

Bu muafiyetler **pay** kuralına aittir. Gölge **animasyonu** yasağı ayrı bir kuraldır ve
istisnası yoktur (§5.4 → M6): scrollbar thumb'ının halesi bu yüzden duruk kalır, yalnız
dolgu rengi geçer.

**Glow ve kaydırma.** Glow, kaydırılabilir ya da sanallaştırılmış tekrar eden öğeye (liste
satırı, tablo satırı, grid hücresi) verilmez; kapsayıcı panele verilir ve satır kenarlıkla
ayrılır. WPF'te tekrar eden öğede `DropShadowEffect` **mutlak yasaktır**. Sayı eşiği yok —
gerekçe, kapsam tablosu ve ölçüm reçetesi `references/motion.md` **M15**'te.
SKILL-2-BITIR

Ayrıca `SKILL §5.4`'teki **mikro etkileşim tavanları** tablosuna bir satır eklenir:

| Kaydırma | tekil yüzeyde duruk glow | tekrar eden öğede glow, WPF `DropShadowEffect` → glow kapsayıcı panele | M15 |

Ve `SKILL §6` (sık yapılan hatalar) listesine bir satır:

- Liste/tablo satırına glow vermek → glow kapsayıcı panele, satır kenarlıkla ayrılır (M15)

### 3 · K2 neden "ölçüm reçetesi" olarak karşılandı

16 ms CI'a bağlanamıyor: WPF bu depoda çalıştırılamıyor, CSS için başsız tarayıcı gerekiyor
ve tarayıcı yok. "≤ 16 ms ölçüldü" yazmak, ölçülmemiş bir sayıyı ölçülmüş göstermek olurdu.
Bu yüzden K2 **eşik + reçete** olarak yazıldı: eşik standardın eşiğidir, ölçümü teslim eden
proje yapar. Reçete M15'te iki platform için ayrı ayrı duruyor (DevTools Performance, CPU
kısma 4×; `CompositionTarget.Rendering` delta'sı, 95. yüzdelik).

### 4 · K5 — ölçülen ve ölçülmeyen

**Ölçüldü** (`node test/u8-glow.js`, envanter her koşuda yeniden sayılır, sabit sayı yok):

| Ne | Sayı |
|---|---|
| `theme.css` kural / bildirim / keyframe adımı | 43 / 192 / 2 |
| `--tk-glow-*` tokenı | 4 |
| glow taşıyan seçici | 5 (`.tk-hero`, `.tk-btn-primary`, `.tk-btn-danger`, `::-webkit-scrollbar-thumb`, `::-webkit-scrollbar-thumb:hover`) |
| `backdrop-filter` | 1 (`.tk-panel`) |
| tekrar eden öğeye düşen glow | 0 |
| geçişte `box-shadow`/`filter` | 0 |

**Ölçülmedi:** kaydırmada kare süresi. Sebep yukarıda (madde 3). Bu satır rapora
"ölçülmedi" diye geçer, tahmin edilmez.

### 5 · Testin canlı olduğunun kanıtı

Test yalnız "geçti" demiyor; sekiz mutasyona karşı denendi ve **sekizinde de düştü**:
token değeri değişti, glow tablo satırına verildi, glow `-row` sınıfına verildi,
`box-shadow` geçişe kondu, `transition: all` yazıldı, ikinci `backdrop-filter` eklendi,
keyframe içine gölge kondu, token silindi. Mutasyonlar `theme.css`'in scratchpad
kopyasında yapıldı — depodaki dosyaya dokunulmadı (`theme.css` U4'ün).

Ayrıca her desen kendi birim testini taşıyor: `glowMu` 13 örnek üzerinde
(7 glow / 6 glow değil), tekrar eden öğe deseni 8 ihlal + 6 temiz seçici üzerinde, ve
"sıfır eşleşme" hâlleri (`T2.0`, `T3.0`, `S2`–`S5`) testi düşürüyor — bozuk bir regex
sessizce geçemiyor.

### 6 · T0'a not — kendim çözmediğim üç şey

1. **Sözleşmenin `## Kabul kriteri` bölümü dosyada yoktu.** `## Amaç` 28. satırda cümle
   ortasında kesilip `## Konsey` başlamış; `## Kapsam ve dalga` ve K1–K5 + SON/CHECK
   bloğu silinmiş. İkisini de `git show HEAD~2` sürümünden birebir geri koydum, Konsey
   bölümüne dokunmadım. `U7` de aynı hasarı bildirmiş — muhtemelen Konsey bölümünü yazan
   adım toplu olarak üzerine yazdı, tek sözleşmelik kaza değil.
2. **Tur ortasında çalışma ağacı bir kez sıfırlandı** (`reflog`: `reset: moving to HEAD`);
   `motion.md` ve `test/u8-glow.js` diskten kayboldu, sonra geri geldi. İşi yeniden
   yazdım, sonuç yerinde. Sıfırlamayı kimin yaptığını bilmiyorum.
3. **24px pay kuralının bir kaynağı yok.** Depoda tek geçtiği yer `SKILL §4`'teki
   *"12px'lik bir hale 24px boşluk ister (§8)"* cümlesi; `§8` ve `layout.md` böyle bir
   kural taşımıyor, `layout.md`'de "glow" kelimesi hiç geçmiyor. Yani yukarıdaki metin
   kuralı **taşımıyor, ilk kez yazıyor**. 24 sayısı aralık merdiveninin (4/8/12/16/24) en
   üst basamağı olduğu için tutarlı ama **ölçülmüş değil**. Kuralı yazdım çünkü K4 onu
   varsayıyor; sayıyı değiştirmek gerekiyorsa karar senin, ben ölçemediğim sayıyı
   seçmiyorum.

Çelişki bulmadım: scrollbar yorumundaki "istisnasız" ifadesi **animasyon** yasağına ait,
muafiyet ise **pay** kuralına. İkisi ayrı cümlelerde tutuldu.
