---
id: Y6
title: Yüzeyden çıkan komut adlarının canlı koddan temizlenmesi + eşik testi
role: scribe
depends: [Y5]
owns: [teknesyum/scripts/ozel.js, teknesyum/scripts/beep.js, teknesyum/scripts/premium.js, teknesyum/scripts/oturum.js, teknesyum/skills/relay/SKILL.md, teknesyum/skills/relay/SETTINGS.md, teknesyum/skills/relay/references/protocol.md, teknesyum/skills/relay/references/rele-akisi.md, teknesyum/skills/relay/references/multi-session.md, test/olcum-dogrula.js]
side_effects: []
risk: düşük
status: blocked
round: 1
---

# Y6 — Olmayan komutu söyleme

## Sorun

Y5 altı komutu sabit yüzeyden çıkardı. Betikler duruyor, erişim yolları `help.md`
gövdesinde yazılı ve test onları tutuyor — **işlev kaybolmadı.** Ama canlı kod
kullanıcıya hâlâ **olmayan komutun adını** söylüyor. Denetçi ~41 yer saydı:

- `teknesyum/scripts/ozel.js`: 319, 327, 359, 366, 406, 413, 423, 437, 455, 475, 483,
  546, 608, 618, 647-654, 674 — `/ozel ekle`, `/ozel pusla`, `/ozel yardim` basıyor
- `teknesyum/scripts/beep.js`: 195, 234, 259
- `teknesyum/scripts/premium.js`: 394 — `/premium durum` çıktısında "`/autocompact` ile bağla"
- `teknesyum/scripts/oturum.js`: 1513
- `teknesyum/skills/relay/SKILL.md`: 151, 188, 196, 349
- `teknesyum/skills/relay/SETTINGS.md`: 92, 145, 163, 168
- `teknesyum/skills/relay/references/protocol.md`: 165, 428
- `rele-akisi.md`: 188 · `multi-session.md`: 233

Kullanıcı `/ozel ekle` yazarsa hiçbir şey olmaz. Bu, özelliğin kaybolmasından farklı
ama ondan daha sinsi: sistem yanlış talimat veriyor.

## İş

### 1. Her geçişi doğru çağrı yoluna çevir

| Eski | Yeni |
|---|---|
| `/ozel <altkomut>` | `node <eklenti>/scripts/ozel.js <altkomut>` |
| `/beep <arg>` | `node <eklenti>/scripts/beep.js <arg>` |
| `/autocompact` | `node <eklenti>/scripts/premium.js autocompact` |
| `/report` | `/update` |
| `/rcadvanced` | `/rc --gelismis` |
| `/rcall` | `/rc --hepsi` |

`<eklenti>` yazımı `help.md`'dekiyle **aynı** olsun — orada `${CLAUDE_PLUGIN_ROOT}`,
çözülmezse `~/.claude/plugins/**/teknesyum` deniyor. İki farklı yazım kullanma.

Betiğin kendi `yardim` çıktısında komut adı geçiyorsa orası da düzelir: kullanıcı o
çıktıyı okuyup kopyalayacak.

**Dikkat:** `docs/`, `CHANGELOG.md` ve `_coplik/` altına dokunma. CHANGELOG geçmiş
sürümleri anlatıyor, isim değiştirmek tarihi yeniden yazmaktır.

### 2. Eşiği test tutsun

Sabit yüzey bugün 1.396 token, eşik 1.400 — **marj 4 token.** Herhangi bir
`description` birkaç kelime uzarsa kriter sessizce düşer ve kimse görmez.

`test/olcum-dogrula.js` içine eşik testi ekle: `scripts/olcum/istem-yuku.js --json`
çıktısındaki `oturumdaBirKez.token` **1.400'ü geçerse test düşsün.** Hata mesajı
kaç token aşıldığını ve hangi tanımın en uzun olduğunu yazsın — düşen kişi nereye
bakacağını bilsin.

### 3. Yeni kırık ipucunu test yakalasın

`test/run.js:264-282`'deki "eski ad" listesi bu altı adı içeremez, çünkü `help.md`
onları anmak zorunda. Bunun yerine `test/olcum-dogrula.js` içine ayrı bir kontrol:
`teknesyum/scripts/` ve `teknesyum/skills/` altında `/ozel`, `/beep`, `/autocompact`,
`/report`, `/rcall`, `/rcadvanced` **talimat olarak** geçmesin. `help.md` ve
`_coplik/` muaf.

## Kabul kriterleri

1. Listelenen ~41 geçişin hepsi doğru çağrı yoluna çevrildi; `teknesyum/scripts/` ve
   `teknesyum/skills/` altında olmayan komut adı talimat olarak geçmiyor
2. Eşik testi var ve 1.400'ü aşınca düşüyor — testi kasten aşırarak kırmızı gördüğünü
   rapora yaz, sonra geri al
3. `node test/run.js` ve `node test/all.js` yeşil — **ham çıktı rapora**
4. `oturumdaBirKez.token` ≤ 1.400 olarak kaldı
5. `docs/`, `CHANGELOG.md`, `_coplik/` ve `teknesyum/commands/` dosyalarına dokunulmadı

## Kayıt noktası

İş 1 — tamam. Sözleşmede sayılan geçişlerin hepsi doğru çağrı yoluna çevrildi:

- `teknesyum/scripts/ozel.js`: 319, 327, 359, 366, 406, 413, 423, 437, 455, 475, 483,
  546, 608, 618, 674 → `node <eklenti>/scripts/ozel.js <altkomut>`. `yardim()` blokunda
  (647-654) her satırın başına tam yolu koymak sütunu 30 karakter kaydırıyordu; blok
  yerine `Çağrı: node <eklenti>/scripts/ozel.js <altkomut>` başlığı + altkomut listesi
  yazıldı, `<eklenti>` tanımı help.md'deki cümlenin aynısı.
- `teknesyum/scripts/beep.js`: 195, 234, 259 → `node <eklenti>/scripts/beep.js`
- `teknesyum/scripts/premium.js`: 394 → `node <eklenti>/scripts/premium.js autocompact`
- `teknesyum/scripts/oturum.js`: 1513 → `node <eklenti>/scripts/ozel.js kur`
- `SKILL.md` 151/188/196/349, `SETTINGS.md` 92, `protocol.md` 165/428,
  `rele-akisi.md` 188, `multi-session.md` 233 → `/update`
- `SETTINGS.md` 145/163/168 → `node <eklenti>/scripts/premium.js autocompact`

Kod yorumlarına dokunulmadı: `ozel.js` 13 ve 390, `oturum.js` 1128 içinde eski ad geçiyor
ama bunlar kullanıcıya basılan metin değil, sözleşmenin satır listesinde de yok.
`teknesyum/scripts/statusline.js:165` ve `teknesyum/scripts/rc.js:99` de aynı durumda ve
zaten owns dışında.

İş 2 ve 3 — tamam, `test/olcum-dogrula.js` içinde. Eşik testi `--json` çıktısındaki
`oturumdaBirKez.token` > 1400 olunca düşüyor; hata satırı kaç token aşıldığını ve en uzun
tanımı yazıyor (enjeksiyon grubu hariç tutuldu — o kalem `oturumdaBirKez`'e girmiyor).
Olmayan-komut kontrolü `teknesyum/scripts/` ve `teknesyum/skills/` altını tarıyor; yorum
satırları ve `require`/import satırları muaf, `_coplik/` ve `node_modules` atlanıyor.

### Kabul 2 — kırmızı kanıtı

Eşiği geçici olarak 1300'e indirip koştum (yüzeyi büyütmek owns dışındaki description
alanlarına dokunmayı gerektirirdi, aynı dal aynı şekilde tetikleniyor):

```
GECTI: iki kip ayni toplami uretiyor

Sabit yüzey eşiği — KALDI: oturumdaBirKez.token 1396, eşik 1300 (96 token aşıldı).
En uzun tanım: Skiller → relay (204 token). Önce oraya bak.
Olmayan komut adı — GEÇTİ (scripts/ ve skills/ temiz)
EXIT=1
```

Eşik 1400'e geri alındı:

```
GECTI: iki kip ayni toplami uretiyor
Sabit yüzey eşiği — GEÇTİ (1396 ≤ 1400)
Olmayan komut adı — GEÇTİ (scripts/ ve skills/ temiz)
Ölçüm doğrulaması — GEÇTİ
EXIT=0
```

Kabul 4 karşılandı: `oturumdaBirKez.token` = 1396.

### Engel — kabul 3 karşılanmıyor

`node test/run.js` → `⨯ KALDI 572/573`. Düşen tek doğrulama:

```
  ⨯ oturum kaydi varken ciplak beep sessiz golgelemeyi uc satirda soyler
      "/beep this sil" yok — gelen: ... 
Bu sohbeti de geneline döndürmek için: node <eklenti>/scripts/beep.js this sil
```

`test/run.js:8043` şu satırı içeriyor: `icerir(r.out, '/beep this sil');` — yani test,
sözleşmenin İş 1'de kaldırmamı istediği eski komut adını çıktıda arıyor. `test/run.js`
`owns` listemde değil, dokunmadım. `node test/all.js` de aynı sebeple `⨯ 1 suite düştü`
veriyor; başka düşen suite yok.

Karar T0'ın: ya `test/run.js:8043` beklentisi yeni çağrı yoluna güncellenir (bir satır),
ya da `beep.js:259` eski adla bırakılır.

## T0 kanit eki — 27.08.2026, denetcinin iki "kanitsiz" maddesi icin

Denetci K3 ve K5'i kanitsiz isaretledi: duzeltme sonrasi `test/all.js` ham ciktisi ve
`git diff --name-only` verilmemisti. Ikisi de asagida.

### K3 — tam takim

`node test/run.js` → **573/573 GECTI**
`node test/all.js` → son satir: **`✓ 10 suite gecti`** — dusen suite yok.

Not: `test/run.js:8074` iddiasi yapicinin owns listesinde degildi. T0 duzeltti:
`icerir(r.out, '/beep this sil')` → `icerir(r.out, 'beep.js this sil')`. `beep.js:259`
artik bu dizgiyi basiyor. Yapici owns disina cikmadi; ayni dosya o sirada Y7'de de
acikti, tek satirlik duzeltme bile cakisirdi.

### K5 — yasak yollara dokunulmadi

`git status --short` (calisma agaci, duzeltme sonrasi):

```
 M .claude/relay/contracts/Y6.md
 M teknesyum/hooks/dil.js
 M teknesyum/hooks/relay-watch.js
 M teknesyum/scripts/beep.js
 M teknesyum/scripts/oturum.js
 M teknesyum/scripts/ozel.js
 M teknesyum/scripts/premium.js
 M teknesyum/skills/relay/SETTINGS.md
 M teknesyum/skills/relay/SKILL.md
 M teknesyum/skills/relay/references/multi-session.md
 M teknesyum/skills/relay/references/protocol.md
 M teknesyum/skills/relay/references/rele-akisi.md
 M test/olcum-dogrula.js
 M test/run.js
?? .claude/relay/contracts/Y7.md
?? docs/RAPOR-100M.md
?? teknesyum/skills/relay/references/premium.md
```

Y6'nin dokundugu her dosya `owns` listesinde. Listede olmayanlarin sahibi:
`dil.js` + `relay-watch.js` T0 (banner satiri kaldirildi), `test/run.js` T0 (beep
duzeltmesi) + Y7, `premium.md` + `Y7.md` Y7, `RAPOR-100M.md` T0.

**`docs/`, `CHANGELOG.md`, `_coplik/`, `teknesyum/commands/` altinda tek degisiklik
yok** — kabul 5 karsilandi.

## Denetim borclari — dordu de kayda gecti

Denetci dort BORC satiri yazdi. Hicbiri kritik degil, bugunku agacta yanlis cikti
uretmiyorlar; kapanana kadar burada duruyorlar:

1. `test/olcum-dogrula.js:88` — `yorumMu` `#` ve `*` ile baslayan satirlari atliyor.
   `.md` dosyalarinda bunlar baslik ve liste imi: 22 dosyada 30 satir bu yolla muaf.
   Bugun o satirlarin hicbirinde alti addan biri gecmiyor.
2. `test/olcum-dogrula.js:8` — `OLMAYAN` dizisi elle yazilmis, `help.md` tablosundan
   turetilmiyor. Yuzeyden yeni bir komut cikarsa kontrol sessizce kapsamaz.
3. `test/olcum-dogrula.js:92` — `help.md` muafiyeti ada gore degil klasore gore:
   yalniz `scripts/` ve `skills/` taraniyor, `help.md` `commands/` altinda oldugu icin
   kazara muaf. Sozlesme "help.md muaf" diyordu; kodda oyle bir kural yok.
4. `test/olcum-dogrula.js:89` — `(?![\w-])` sonrasinda `/` serbest; `/report/` gibi bir
   yol parcasi yanlis yakalanabilir. Su an boyle bir satir yok.
