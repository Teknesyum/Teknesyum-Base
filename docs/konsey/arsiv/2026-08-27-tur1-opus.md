# Koşu 2026-08-27 · tur 1 · opus (üye) — birebir metin

Soru: yarınki bench'ten önce kırılganlık taraması. Sessiz başarısızlıklar, ölçüm
aletlerinin güvenilirliği, durum dosyalarının tutarlılığı, test kapsamının yalanı.

---

## Kavrayış

Brifingle sözleşmem çelişiyor: brifing beş numaralı bulgu başlığı istiyor, sözleşmem beş sabit
başlık dışına çıkmamı yasaklıyor. Sözleşmeye uydum — bulgular `## Riskler` altında `dosya:satır`
ile duruyor, plan onları kapatma sırasıdır.

İş: yarınki bench'ten önce "yanlış sonucu doğru gibi gösteren" yerleri kapatmak. Üç sınıf buldum,
üçü de canlı: (a) mühür zincirinde **kanıt yoksa geçir** kararı, (b) ölçüm aletlerinin birbiriyle
çelişen tanımları, (c) boş kümede "hepsi geçti" diyen rapor.

Beklemediğim durumlar, ikisi de sessiz geçilmedi: **Bash bu oturumda kapalı** — `node --check` ve
`git log` koşamadım, bütün hükümler dosya okuma + grep kanıtına dayanıyor, çalışma zamanı
doğrulaması yok. `docs/taramalar/RAPOR.md` var ve okundu; içindeki alınan fikirlerden ikisi bu
plana doğrudan giriyor (aider "edit sonrası otomatik denetim döngüsü" -> A1'in kapı testi;
cline "engellemeyle birlikte gerekçe" -> A1'de `_sorun.log` yerine engel mesajı). Kalan maddeler
kırılganlıkla ilgisiz olduğu için girmedi.

## Plan

**A0 — Bench'i bugün yalancı yapan üç satır (builder, 1 sözleşme, bench'ten önce şart).**
Owns: `scripts/bench/topla.js`, `scripts/bench/pano.js`, `scripts/olcum/istem-yuku.js`.
1. `topla.js:559` `kosular.every(...)` boş dizide `true` — bütün koşular elenirse rapor
   "dört koşulun tamamı dört görevi de geçti" basar. `main()` yalnız `hepsi.length`'i (612)
   koruyor, `kosular.length`'i değil.
2. `pano.js:202,216` `verimSatirlari(await topla())` — **süzülmemiş**; `topla.js:616` süzüyor.
   Panodaki verim tablosu geçersiz koşuları ortalamaya katıyor, nihai rapor katmıyor.
3. `istem-yuku.js:145` `/^status:s*(w+)/m` — ters bölüler düşmüş, hiçbir zaman eşleşmez;
   `acikSozlesmeVar()` daima `false`, "kapı hatalı kapalı" uyarısı hiç ateşlenemez.
Kabul: boş `bench/sonuc/` ile `topla.js` koşulduğunda §5 "başarı" satırı **"geçerli koşu yok"**
yazar; pano ile rapor aynı girdiden aynı verim satırını üretir; `status: active` içeren sahte
sözleşmeyle `istem-yuku.js` uyarıyı basar.

**A1 — Mühür zincirinin "kanıt yoksa geçir" kararı.**
Owns: `teknesyum/hooks/denetim-kaydi.js`, `teknesyum/hooks/contract-guard.js`.
`denetciSaglam` (`denetim-kaydi.js:102-104`) `live/<runId>.json` okunamazsa `null` = "sağlam"
döner. Uydurma bir `auditorRunId` hem burada hem `contract-guard.js:92`'de geçer. Guard hiç
değilse `_sorun.log`'a yazıyor, `denetciSaglam` **tamamen sessiz**. Karar: `contract.js complete`
yolunda kayıt yoksa **reddet**; guard'daki gevşeklik kalsın, o Write yolunu koruyor.
Aynı sözleşmede: `dosyaOzeti:40` okunamayan yolu boş buffer sayıyor — silinmiş dosya ile boş
dosya aynı hash'i veriyor, `owns` girdisi bir **dizin** ise EISDIR yutuluyor.

**A2 — Defter devralma arka kapısı.** Owns: `teknesyum/hooks/denetim-kaydi.js`.
`defterKur:153-166` defter yoksa `done/` altındaki her şeyi `devralindi` diye meşrulaştırıyor ve
`doneDenetle:189` **denetimin kendisi** onu çağırıyor: defteri sil -> `contract.js audit` temiz der.
Kanıt diskte: `.claude/relay/audits/defter.jsonl` satır 1, 19, 20, 21 — `T2` dört kez
`devralindi`, `T2`'nin hiç `.used.json` denetim kaydı yok, ama `done/T2.md` duruyor.
Karar: devralma yalnız açık bayrakla (`--devral`) ve tek seferlik damgayla; `audit` komutu asla
defter yaratmaz. Mevcut test `run.js:5044` bugünkü davranışı **doğru** sayıyor — tersine çevrilecek.

**A3 — Dalga çakışması ve tur varsayılanı.** Owns: `teknesyum/scripts/contract.js`.
`contract.js:62` yeni dalganın `T7`'sini eski dalganın `done/T7.md`'siyle karıştırıyor.
`:71` `alanDegeri('round', govde) || '1'` — `round:` satırı eksikse sessizce tur 1 varsayıyor.

**A4 — Bench koşu hijyeni.** Owns: `scripts/bench/kos.js`.
`kos.js:113-116` `c.kill()` süreç ağacını öldürmüyor — `defter.jsonl:41`'de B1 borcu, kapanmamış.
Öksüz `claude` süreci kotayı yemeye devam ediyor; `transkriptBul:330-335` "en yeni .jsonl"i
seçtiği için sonraki koşunun transkriptini yanlış koşuya bağlayabilir.

**A5 — Ölçüm tanımlarının birleştirilmesi (bench'ten sonra).**
`taban.js:11` katsayı **3,6**, `istem-yuku.js:17` **2,492**. `taban.js`'in ürettiği her token
sütunu ~%30 düşük. `konsey-maliyet.js:39-48` `requestId` ile tekilleştirmiyor -> konsey maliyeti
şişik. `topla.js:225` `filter(gecerli)`, `istatistik.js:225` `filter(gecerli !== false)` -> aynı
veriden iki farklı n.

Sıra: A0 -> (A1, A2, A3, A4 paralel) -> A5.

## Riskler

- `topla.js:438` `bul(g,d)` görev+koşul için **ilk** koşuyu buluyor; `--tekrar 3` ile §1 tablosu
  r2 ve r3'ü sessizce gizler.
- `topla.js:161-171` `LIMIT_ZAYIF = /session limit/i` **bütün transkript metninde** aranıyor.
  Model `docs/BENCH-SONUC.md` errata bloğunu okursa koşu geçersiz damgalanır.
- `harnessKarsilastir:231` `harness <= 0 || n < 2` ise satır atlanıyor, rapor "En büyük sapma %-"
  yazıp geçiyor; eşik %5 hiç sınanmamış olur.
- `ortak.js:52-62` `yaz()` yazma hatasını tamamen yutuyor. Mührün baktığı dosya bu.
- `defterOku:141` bozuk JSONL satırını sessizce atıyor. BOM'lu ilk satır -> sözleşme defterden
  düşer -> `audit` onu yetkisiz sayar.
- `kayitTuket:119-121` `unlinkSync` hatası yutuluyor. Windows'ta kilitliyse `.used.json` yazılır
  ama orijinal kalır; ikinci `complete` tekrar oynatılır.
- `topla.js:334` `kusur||0` — `kusur` null (bilinmiyor) ise sıfır gibi toplanıyor.
- `pano.js:223` `process.exit` boruda çıktıyı kesebilir.
- **Plan riski:** A1 ve A2 aynı dosyayı sahipleniyor (`denetim-kaydi.js`); paralel koşarlarsa
  `diffHash` çakışır. `defter.jsonl:49`'da yazılı O5/O6 tasarım borcu, çözülmemiş.

## Ayrım noktaları

- **Bench'ten önce ne kadar dokunulur.** Yalnız A0'ı seçtim. Gerekçe: A1-A3 mühür zincirini
  değiştiriyor ve mühür zinciri bench koşusunun *içinde* çalışmıyor (headless, sözleşme
  tamamlama yok). Bench gecesi mühür zincirine dokunmanın kendisi risk.
- **`denetciSaglam` kayıt yoksa: reddet mi, uyar mı.** Canonical yolda reddetmeyi seçtim,
  guard'ı olduğu gibi bıraktım.
- **Devralma tamamen kaldırılsın mı.** Bayrak arkasına almayı seçtim; kaldırmak mevcut 39
  `done/` sözleşmesini bir anda yetkisiz yapar.
- **Katsayı birleştirmesi bench'ten önce mi.** Sonra dedim. Emin değilim: `topla.js:100`
  `tokenTahmini`'yi de dışa veriyor, başka bir çağıran varsa yanılıyorum — Bash olmadığı için
  tam çağrı zincirini izleyemedim.

## Reddettiklerim

- `live/` altındaki dosyaları temizlemek — kırılganlığın kaynağı dosya sayısı değil, kayıt
  *yokken* geçiren mantık. Önce A1, sonra arşivleme.
- Bütün `catch {}` bloklarını gerekçelendirmek — 90+ tane, çoğu sonuç üretmeyen yerde.
- Bench'i tekrar sayısını artırarak sağlamlaştırmak — `istatistik.js:352` zaten yazıyor: n=3'te
  en küçük iki yönlü p 0,25.
- Yeni bir "sağlamlık" kancası eklemek — sorun eksik kanca değil, var olan kancaların kanıt
  bulamayınca açık tarafa düşmesi.
- `topla.js:85-113` `new Function` refaktörü — bench gecesi getirisi yok.
